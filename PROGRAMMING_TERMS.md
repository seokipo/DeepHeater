# 프로그램 용어 사전 (PROGRAMMING_TERMS.md)

본 문서에는 심부발열기 디스플레이 보드 제어 프로젝트를 수행하며 사용된 주요 기술 용어와 개념들을 정의하고 기록합니다.

---

## 1. HFINTOSC (High-Frequency Internal Oscillator)
- **개념**: 외부에 크리스탈이나 레조네이터(Resonator) 같은 클럭 소스를 연결하지 않고, MCU 내부에서 독립적으로 고주파 클럭(최대 32MHz)을 생성하는 내부 고주파 발진기 모듈입니다.
- **용도**: PIC16F18855 시스템 초기화 시 시스템 동작 주파수(`_XTAL_FREQ = 32000000`)를 설정하기 위한 메인 클럭 소스로 사용되었습니다.

## 2. PNP 트랜지스터 드라이버 (PNP Transistor Driver)
- **개념**: PNP형 BJT(양극성 접합 트랜지스터)의 특성을 이용하여, 베이스(Base) 단자에 LOW(0V) 신호가 인가될 때 이미터(Emitter)에서 컬렉터(Collector)로 전류가 흐르도록 동작하는 스위칭 구동 회로입니다.
- **용도**: FND 공통 애노드(Common Anode) 단자 및 고전력 LED 제어 라인(DIG1, DIG2, LED_DRV)의 전원 공급을 MCU 포트 출력 `LOW` 신호로 온/오프 제어하기 위해 하드웨어 드라이버로 적용되었습니다.

## 3. 키 매트릭스 (Key Matrix)
- **개념**: 다수의 키 스위치를 행(Row)과 열(Column)의 교차선에 배열하여, 최소한의 I/O 핀만을 점유하면서 여러 개의 키 입력을 개별 스캔하고 판독하는 회로 설계 기법입니다.
- **용도**: FND 세그먼트 출력 라인(RB0~RB5)을 스캔 출력선으로 공유하고, RC7을 키 입력선으로 활용하여 여러 개의 설정 버튼 입력을 효과적으로 판독하는 데 사용되었습니다.

## 4. PPS (Peripheral Pin Select)
- **개념**: 디바이스 내부의 디지털 주변장치(UART, PWM, SPI 등)의 입출력 핀을 고정된 물리 핀이 아닌, 개발자가 소프트웨어 레지스터 설정을 통해 원하는 물리 I/O 핀에 동적으로 매핑할 수 있게 지원하는 주변장치 핀 선택 기능입니다.
- **용도**: EUSART 직렬 통신 핀(TX, RX) 및 부저용 CCP1 PWM 출력, 히터용 PWM6 출력을 각각 지정된 포트 핀에 유연하게 할당하는 데 사용되었습니다.

## 5. EUSART (Enhanced Universal Synchronous Asynchronous Receiver Transmitter)
- **개념**: 동기식 및 비동기식 시리얼 통신을 모두 지원하며, 내부에 보오율 발생기(Baud Rate Generator) 및 자동 오토바드 감지 기능을 탑재한 강화된 범용 비동기 송수신 시리얼 통신 모듈입니다.
- **용도**: PC나 외부 제어 장치와의 디버깅 정보 교환을 위해 9600bps 보오율로 비동기 UART 통신 환경을 구축하는 데 적용되었습니다.

## 6. 디스플레이 동적 스캔 (Dynamic Scanning)
- **개념**: 다수의 디스플레이 자릿수(FND, LED)를 동시에 켜지 않고, 아주 빠른 속도로 자릿수를 순차적으로 점멸하여 사람의 눈에는 잔상 효과(Persistance of Vision)로 인해 모든 자릿수가 동시에 켜져 있는 것처럼 보이게 제어하는 기법입니다.
- **용도**: 좌측 FND, 우측 FND, 그리고 표시 LED 라인을 약 1ms~2ms 간격으로 순차 구동하여 전류 소모를 줄이면서 화면을 정상 표시하는 데 사용되었습니다.

## 7. 고스트 현상 (Ghosting)
- **개념**: 동적 스캔 구동 시, 이전 자릿수의 출력 데이터 잔재가 다음 자릿수가 켜지는 시점까지 지워지지 않고 남아있어서 다른 자릿수에 미세한 번짐이나 잔상이 겹쳐 보이는 시각적 글리치 현상입니다.
- **용도**: 자릿수 스위칭 제어 직전에 세그먼트 포트 출력 데이터(`LATB`)를 소등 상태(`0xFF`)로 초기화하여 고스트 현상을 원천 차단했습니다.

## 8. 데드 타임 (Dead Time)
- **개념**: 전기적 스위칭 소자(트랜지스터, MOSFET)가 물리적으로 완전히 차단(Turn-off)되는 데 걸리는 반응 지연 시간 동안, 오동작이나 전류 충돌을 방지하기 위해 강제로 부여하는 미세한 시간 지연(지연 마진)입니다.
- **용도**: FND 동적 스캔 시 자릿수 스위칭 사이에 약 50us의 데드 타임 딜레이를 삽입하여 물리적 트랜지스터의 차단 지연으로 인한 화면 번짐을 억제하는 데 적용되었습니다.

## 9. 3단계 동적 스캔 (3-Phase Dynamic Scanning)
- **개념**: FND의 세그먼트 캐소드 라인과 인디케이터용 개별 LED의 캐소드 라인이 포트 핀을 물리적으로 공유할 때, 간섭 현상을 완전히 배제하기 위해 FND 1번 슬롯, FND 2번 슬롯, 개별 LED 슬롯의 3개 위상으로 스캔 주기를 완벽히 분리해 구동하는 동적 스캔 방식입니다.
- **용도**: 세그먼트 포트(`PORTB`)를 공유하는 우측 FND, 좌측 FND, 개별 레벨 LED를 1ms씩 순차 구동하는 3단계 스캔 시퀀스를 유지하기 위해 적용되었습니다.

## 10. 백그라운드 카운트다운 타이머 (Background Countdown Timer)
- **개념**: 메인 스캔 루프 내부에서 동기식 대기 딜레이를 쓰지 않고, 매 루프의 경과 시간을 감시하여 지정된 초 단위(1초 등) 흐름을 감쇠해 나가는 비동기식 소프트웨어 타이머 설계 패턴입니다.
- **용도**: 히터 치료 동작 중 메인 디스플레이 동작 정지 없이 지정된 동작 남은 시간(분/초)을 1초마다 정확하게 깎아 나가는 데 적용되었습니다.

## 11. 디바운스 (Debounce)
- **개념**: 기계식 스위치를 누르거나 놓을 때 접점의 미세한 흔들림(채터링, Chattering)으로 인해 매우 짧은 시간 동안 여러 번 눌린 것으로 오인하는 현상을 차단하기 위해, 일정한 시간 마진 동안 키 상태 변동을 필터링하는 기법입니다.
- **용도**: 키 입력 감지 시 연속 루프 횟수를 카운트하여 최소 30ms 이상의 신뢰성 있는 상태 유지 시에만 실제 키 입력으로 인정하도록 필터링하는 로직에 적용되었습니다.

## 12. 협력적 멀티태스킹 / 비동기 제어 (Cooperative Multitasking)
- **개념**: 단일 스레드로 동작하는 MCU 환경에서, 긴 대기 딜레이 함수(`__delay_ms`) 없이 각 제어 모듈이 짧은 연산 후 양보하는 구조로 메인 루프를 빠르게 반복 실행시켜 다수의 태스크가 병렬로 구동되는 것처럼 구현하는 기법입니다.
- **용도**: 부저 비프음 출력, FND 점멸 제어, 키 스캔 등의 독립된 태스크들이 하나의 메인 루프 안에서 지연 없이 병행 처리되도록 하는 구조적 기본 설계로 사용되었습니다.

## 13. 키 자동 반복 / 오토 리피트 (Auto-Repeat)
- **개념**: 사용자가 키 스위치를 특정 시간(임계 시간) 이상 지속해서 누르고 있으면, 키를 반복적으로 누른 것과 동일하게 처리하여 설정 값을 연속으로 증가시키거나 감소시키는 키보드 입력 기법입니다.
- **용도**: 시간이나 온도를 설정할 때 올림/내림 버튼을 꾹 누르고 있으면 초기 홀드 시간(500ms) 이후 100ms 간격으로 설정 값이 연속 자동 변경되도록 최적화하는 데 적용되었습니다.

## 14. 약한 풀업 저항 / WPU (Weak Pull-up)
- **개념**: MCU 내부에 구현된 고저항 풀업 기능을 활성화하여, 외부 저항 없이도 핀이 아무 상태도 아닐 때(플로팅 상태) 기본 전압 레벨을 HIGH로 안정되게 유지해주는 소프트웨어 제어형 내부 저항입니다.
- **용도**: 키 스캔 입력 핀 및 사용되지 않는 유휴 GPIO 핀들의 입력 바이어스를 HIGH로 유지하여 노이즈 인입을 예방하는 데 사용되었습니다.

## 15. PPS 핀 충돌 및 재매핑 제어 (Peripheral Pin Conflict & Override)
- **개념**: 서로 다른 디지털 기능(예: 부저 PWM 및 히터 PWM)이 동일한 물리 핀에 할당되거나 레지스터 설정이 겹치지 않도록, PPS 레지스터 설정 값을 유일하게 지정하고 런타임에 핀 동작 모드를 명확히 격리 제어하는 기법입니다.
- **용도**: RC0 핀의 부저 CCP1 매핑과 히터 구동 Timer2 제어 공식 충돌을 차단하기 위해 Timer 할당 및 PPS 출력 값을 엄격하게 분리 구성하는 데 사용되었습니다.

## 16. 수동 피에조 부저 (Piezo Buzzer / Passive Buzzer)
- **개념**: 외부에서 일정한 주파수의 사각파(PWM) 신호를 직접 인가해주어야만 해당 주파수에 매칭되는 고유의 진동음을 발생하는 형태의 수동형 부저 소자입니다.
- **용도**: 사용자의 키 입력 시 2.7kHz의 경쾌한 동작 피드백 비프음을 생성하기 위해, PWM 파형을 외부 스위칭 TR을 거쳐 부저 소자에 인가하는 제어 회로로 적용되었습니다.

## 17. 하드웨어 PWM 구동 (Hardware PWM Drive)
- **개념**: 소프트웨어 타이머를 쓰지 않고, MCU 내부에 독립 구현된 CCP/PWM 하드웨어 모듈과 전용 타이머(Timer2/4)를 결합하여 설정된 주파수와 듀티 비(Duty Cycle)를 가진 사각파 신호를 자동으로 핀에 연속 출력하는 방식입니다.
- **용도**: 부저 구동용 2.7kHz 파형 출력 및 히터 제어용 하프 브리지 전력 제어 파형 출력을 안정적으로 생성하기 위해 CCP1 및 PWM6 모듈을 직접 하드웨어 설정해 사용되었습니다.

## 18. PPS 입력 우회 설정 (PPS Input Bypass/Rerouting)
- **개념**: 외부 제어 입력 신호나 통신 라인을 MCU 내부로 받아들일 때, PPS 설정 레지스터를 통해 지정된 물리 핀의 데이터를 내부 모듈(예: EUSART RX)에 할당 우회하는 입력PPS 매핑 기법입니다.
- **용도**: RC7 핀으로 유입되는 시리얼 수신 신호를 EUSART RX 모듈에 바인딩하기 위해 `RXPPS = 0x17` 설정을 구성하는 데 적용되었습니다.

## 19. 동적 입출력 방향 전환 (Dynamic I/O Direction Switching)
- **개념**: 프로그램 실행 중에 특정 GPIO 핀의 입출력 모드 설정 레지스터(TRIS) 값을 동적으로 변경하여 필요 시에는 입력, 필요 시에는 출력 핀으로 실시간 역할을 교체하는 제어 기법입니다.
- **용도**: 키 매트릭스 구동과 세그먼트 표시 제어 간 상호 핀 역할을 변경할 때, 일시적으로 포트 입출력 방향 레지스터(`TRISB`)를 재설정하여 세그먼트 데이터 간섭을 차단하는 데 적용되었습니다.

## 20. 순차적 캐소드 스캔 (Sequential Cathode Scanning)
- **개념**: 여러 개의 디스플레이 자릿수가 캐소드(Cathode) 라인을 공유하고 있을 때, 자릿수 제어 전원을 한 개씩만 번갈아 인가하면서 해당하는 세그먼트(애노드) 데이터를 동기화하여 순서대로 표현하는 동적 스캔 제어의 세부 동작 기법입니다.
- **용도**: LATA1(DIG1), LATA2(DIG2), LATA3(LED_DRV) 스위칭 라인을 차례대로 ON하면서 세그먼트 데이터(`LATB`)를 알맞게 스위칭 표출하는 스캔 설계에 적용되었습니다.

## 21. 잔류 풀업 누설 전류 방지 (Residual Pull-up Leakage Prevention)
- **개념**: 스위칭 트랜지스터가 꺼져(OFF)있는 대기 상태에서, 포트 핀 내부에 활성화되어 있는 풀업 저항이나 I/O 잔류 전압으로 인해 전류가 미세하게 흘러 FND나 LED가 희미하게 불빛을 뿜는 고스트/누설 광 현상을 차단하는 방지책입니다.
- **용도**: 점멸 루프 소등 구간에서 내부 WPU 설정을 초기화하고 포트 레지스터를 완벽하게 클리어하여 미세한 LED 잔상 누설을 제거하는 데 적용되었습니다.

## 22. 물리적 키 매트릭스 상태 일관성 스캔 (Physical Consistency Matrix Scan)
- **개념**: 키 입력 판독 시, 전기적 노이즈나 일시적 접점 이상으로 인한 오판독을 막기 위해 키 스캔 전압을 가한 뒤 아주 미세한 지연 시간(Dead Time)을 확보하여 포트 신호의 물리적 전압 전위가 안정화되었을 때 전위 상태를 읽어 일관성을 확보하는 기법입니다.
- **용도**: 세그먼트 출력 핀을 LOW로 떨어뜨린 후 안정화 지연을 거쳐 RC7 핀의 실제 전압 레벨을 연속 2회 이상 읽어 동일할 때 키 입력으로 받아들이는 안전 필터 설계에 적용되었습니다.

## 23. 외부 풀업 존재 시 내부 WPU 비활성화 (WPU Disable with External Pull-ups)
- **개념**: 회로 설계상 물리적인 외부 풀업 저항이 핀에 이미 장착되어 있는 경우, 불필요한 전류 소모를 억제하고 신호의 상승/하강 임계 특성을 뚜렷하게 보장하기 위해 소프트웨어 레지스터 상에서 해당 핀의 내부 WPU 옵션을 꺼 두는 최적화 조치입니다.
- **용도**: 세그먼트 제어 라인 및 레벨 선택 키에 장착된 10k 외부 저항과의 병렬 간섭을 배제하여 신호의 명확성을 높이기 위해 `WPUB = 0x00` 등으로 비활성화 처리되었습니다.

## 24. 바그래프 디스플레이 표현 (Bar Graph Display Representation)
- **개념**: 단일 픽셀이나 숫자 FND 대신, 여러 개의 개별 지시 LED를 일렬로 배치하여 현재 제어 레벨(세기, 강도)에 비례하는 개수만큼 연속적으로 점등시킴으로써 직관적인 동작 현황을 보여주는 인디케이터 구동 기법입니다.
- **용도**: 심부발열기 출력 강도(Level 1~6)에 매핑하여 LED1부터 LED6까지의 개별 지시등을 비례적으로 채워 켜는 세그먼트 제어 설계에 적용되었습니다.

## 25. 독립 기능 도메인 제어 (Independent Function Domain Control)
- **개념**: 부저 타이머 감쇠 루프와 디스플레이 주기 루프 등 성격이 다른 여러 타이머/카운터의 런타임 제어 단위를 코드상에서 변수나 구조로 엄격하게 분리하여 상호 동작 간섭 및 타이밍 왜곡이 전혀 일어나지 않게 독립 설계하는 소프트웨어 아키텍처입니다.
- **용도**: 히터 치료 잔여 시간 카운트 동작이 부저 작동 주기나 키 입력 채터링 감지 루프의 상태에 영향받지 않고 순수하게 시간 축상에서 독립 감쇠하도록 변수 도메인을 격리하는 데 사용되었습니다.

## 26. 실시간 물리 상태 유지 (Real-time Physical State Holding)
- **개념**: 외부 제어 신호가 차단되거나 메인 구동 조건이 오프 상태로 전환되더라도, 순간적인 상태 기억 변수(State Register) 값을 보존하여 즉각적인 상태 갱신이 보장될 수 있도록 실시간으로 현재의 장치 및 포트 출력 상태 플래그를 정기 갱신/유지하는 처리 기법입니다.
- **용도**: 치료 도중 일시 정지(`is_running = 0`) 시, 기존에 설정되어 있던 강도 레벨(`current_level`)과 하이모드 상태(`is_hi_mode`) 값을 메모리에 보존하여 재개 시 이전 상태에서 즉시 동작을 이어갈 수 있게 구현하는 데 적용되었습니다.

## 27. 대기 화면 동기화 제어 (Standby Display Synchronization)
- **개념**: 치료 동작 대기 상태(Idle) 진입 시 디스플레이 소등 유도와 전력 절감을 위해 일정한 시간 간격(500ms 등)으로 FND 자릿수 인가 핀 전원을 토글하는 점멸 구동 시퀀스를 구현하되, 키 입력이 일어나는 순간 화면 표출 상태로 즉각 화면을 온전하게 동기화해 응답성을 높이는 기법입니다.
- **용도**: 정지 상태일 때 500ms 주기로 FND를 Blink 점멸시키다가, 치료 시작이나 모드 전환 키 입력 시 즉각 상시 점등(`blink_state` 무시) 상태로 FND 구동 위상을 동기 전환하는 로직에 적용되었습니다.

## 28. 비동기 주파수 가변 3중 감쇠 효과음 (Asynchronous Frequency-Switching Triple Decay Tone)
- **개념**: 동기식 딜레이 없이 런타임에 하드웨어 PWM 레지스터 값을 단계적으로 자동 조절하여 음의 피치(Pitch)를 시간에 따라 빠르게 가변 감쇠시켜, 한 번의 키 입력 시 경쾌하고 자연스러운 아날로그풍의 멀티 톤 비프음을 생성하는 비동기식 사운드 가변 기법입니다.
- **용도**: 치료 시작이나 모드 키 입력 시 단순 단음 비프가 아닌, 주파수 레지스터(PR2)를 비동기로 연속 가변 감쇠하여 생성된 3중 감쇠 비프음을 출력하는 데 사용되었습니다.

## 29. 주파수 가변 듀티 매칭 제어 (Frequency-Variable Duty Matching Control)
- **개념**: PWM 동작 주파수를 동적으로 가변할 때, 듀티 비(Duty Cycle)가 주파수 변경에 영향받지 않고 일정한 출력 실효 전력량(50% 등)을 일정하게 출력하도록 주파수 주기 레지스터(PRx) 설정 공식에 비례하여 듀티 레지스터(CCPRxL) 값을 자동 연동 계산 및 보정해주는 매칭 제어입니다.
- **용도**: 부저의 음역대 변경(2.7kHz -> 2.4kHz 등)을 위해 주기를 가변할 때, 부저 사각파의 실효 출력이 항상 50% 듀티가 유지되어 소리 찌그러짐을 방지하도록 하는 듀티 레지스터 값 자동 연동 공식에 적용되었습니다.

## 30. 소프트웨어 플레이스홀더 디자인 패턴 (Software Placeholder Design Pattern)
- **개념**: 향후 추가될 확장 기능(예: 리모컨 디코딩 디버깅 모드 등)을 위해 코드나 데이터 사전에 특정 식별 헤더와 더미 구문을 사전에 할당해 둠으로써, 기존 동작에 아무 영향 없이 신규 모듈을 유연하게 탑재할 수 있게 배려하는 준비 구조 설계 방식입니다.
- **용도**: 프로그램 용어사전에 번호 조각들을 할당해 두거나, `main.c` 내에 디버깅용 CLI 뼈대 루프를 만들어두어 향후의 수정 연계를 돕는 최적화 코딩 패턴으로 사용되었습니다.

## 31. TTL 입력 문턱 전압 선택 (TTL Input Threshold Selection / INLVLA)
- **개념**: MCU 포트 입력 핀의 논리 신호 임계값 판독 기준을 슈미트 트리거(Schmitt Trigger) 수준의 높은 문턱값 대신, 일반적인 0.8V/2.0V 기준의 낮은 TTL 임계값 수준으로 낮추어 노이즈가 유입되어도 로직 인식 충돌을 피할 수 있게 레지스터 값을 지정하는 선택 기능입니다.
- **용도**: 디스플레이 동적 스캔 라인과의 간섭이 있는 키 입력 핀(RC7)의 스위칭 판독 한계를 최적화하기 위해, 레지스터 설정을 정밀화하여 입력 레벨 충돌을 차단하는 데 적용되었습니다.

## 32. Timer1 게이트 제어 비활성화 (Timer1 Gate Control Disable / T1GCON)
- **개념**: Timer1 카운팅 소스가 외부 하드웨어 이벤트 신호에 제어(Gate Control)되지 않고, 지정된 내부 시스템 클럭 소스에 맞춰 항상 독립적이고 중단 없이 정밀하게 타임 카운트를 카운팅하도록 게이트 바인딩을 강제로 끄는 옵션입니다.
- **용도**: 백그라운드 타이머 시간 오차를 차단하기 위해 Timer1 클럭 카운트를 활성화할 때, 불필요한 게이트 입력에 의한 중단을 막기 위해 `T1GCON = 0x00` 설정을 적용하는 데 사용되었습니다.

## 33. 비주얼 FND 디버깅 (Visual FND Debugging)
- **개념**: 직렬 통신 포트나 전문 에뮬레이터 없이, 오직 보드 상에 탑재된 FND 7세그먼트 디스플레이 화면의 빈 자리나 특정 세그먼트 픽셀을 디버깅 출력 플래그 상태창으로 활용하여 내부 오류 코드나 특정 변수 단계를 실시간으로 사용자 눈에 확인시켜 주는 간이 디버깅 기법입니다.
- **용도**: 내부 통신 보오율 초기화 여부나 에러 리셋 신호 유입 단계를 FND 화면 상에 특정 에러 코드(E1, E2 등)나 LED 핀 불빛 점멸 시퀀스로 표출해주는 가독성 디버깅 로직에 적용되었습니다.

## 34. 시리얼 블로킹으로 인한 디스플레이 글리치 (Serial Blocking Display Glitch)
- **개념**: 시리얼 송수신 문자열을 송출할 때 버퍼가 빌 때까지 대기(Polling)하는 동기식 블로킹 코드를 장시간 실행하면 FND 동적 스캔 주기(1ms)가 강제로 멈춰 서서 디스플레이 글리치(화면 깜빡임, 꺼짐, 깨짐) 현상을 유발하는 하드웨어 간섭 오류입니다.
- **용도**: 디버그 시 출력이 너무 길거나 런타임에 동기 송신 시 화면이 툭툭 끊기는 글리치를 차단하기 위해 인터럽트/비동기 버퍼링 방식으로 송신 처리를 격리하는 예방 조치에 적용되었습니다.

## 35. RC 시정수 필터 (RC Time Constant / \tau)
- **개념**: 저항(R)과 커패시터(C)를 조합하여 만든 1차 로우 패스 필터(Low Pass Filter) 회로로, 신호의 고주파 노이즈 성분을 감쇄시키고 아날로그 전압의 변화를 부드럽게 필터링해주는 물리적인 하드웨어 필터 회로입니다.
- **용도**: 치료 강도 레벨 조정 버튼 입력부 및 아날로그 전압 입력부의 급격한 서지나 서브 밀리초 채터링 노이즈를 1차 필터링하기 위해 적용되었습니다.

## 36. 핀 상태 독립 디코딩 (Pin-State-Free Decoding)
- **개념**: 특정 GPIO 핀의 논리 상태 변화를 분석할 때, 주변 장치 구동이나 포트의 다른 비트 상태에 결합되지 않고 오직 독립된 소프트웨어 상태 머신과 이벤트 트리거에 의해서만 로직을 독립적으로 해독(Decoding)해내는 코딩 기법입니다.
- **용도**: 스캔 루프와 공유된 RB0~RB5 포트 핀들의 개별 핀 제어가 키 디코딩 시 영향을 주지 않도록 매트릭스 디코더 변수를 완전히 분리 구현하는 데 적용되었습니다.

## 37. DNP (Do Not Populate / Do Not Place)
- **개념**: 인쇄회로기판(PCB) 상에 회로 패턴 및 패드는 설계되어 있으나, 양산 및 보드 구동 목적상 해당 부품을 실제 기판 위에 납땜하여 실장하지 않고 빈칸으로 비워 두는 부품 미실장 표기법입니다.
- **용도**: 회로 검증 단계에서 예비로 배치했던 커패시터나 점퍼 저항(예: 노이즈 억제용 유휴 부품)을 부품 명세서(BOM) 상에서 제외 처리하는 데 적용되었습니다.

## 38. 소프트웨어 글리치 채터링 방지 데드타임 (Software Glitch Debounce / Chattering Deadtime)
- **개념**: 스위치의 물리적 오작동이나 전기적 노이즈 스파이크로 인해 비정상적으로 극도로 짧은 시간에 연속 유입되는 불규칙 신호(글리치)를 원천 무시하기 위해 스위치 입력 인식 즉시 약 수십ms 동안 추가 스캔을 강제로 정지시키는 데드타임 제어 기법입니다.
- **용도**: 키 입력이 최초 판독되어 인식되는 시점 직후에 연속적인 채터링이나 미세 진동 파형이 겹쳐서 동일한 키가 연속 두 번 눌린 것으로 처리되는 더블 클릭 오류를 방지하기 위해 적용되었습니다.

## 39. 슬루레이트 독립 하강에지 디코딩 (Slew-Rate Independent Falling-Edge-Only Decoding)
- **개념**: 입력 신호의 전압 상승/하강 속도(Slew Rate)가 느려 전위 변화가 비스듬하게 변하더라도, 상태 전환 오류가 없는 하강 에지(Falling Edge) 시점만을 클럭 트리거로 활용하여 디지털 데이터를 정확하게 카운트해 해독해 내는 디코딩 방식입니다.
- **용도**: FND 잔상 방지를 위해 전하 축적이 일어나는 세그먼트 전압 변위에서, 불완전한 전압 전위를 배제하고 하강 순간만을 디텍션하여 신호 상태를 검출하는 데 사용되었습니다.

## 40. 하강에지 간격 기반 NEC Decoding (Falling-Edge Interval-Based NEC Decoding)
- **개념**: 적외선(IR) 리모컨 수신 디코딩 시, 수신 모듈 아웃풋 전압 파형의 하강 에지와 다음 하강 에지 사이의 지속시간(Interval)을 마이크로초 단위 타이머로 정밀 측정하여 리더(Leader), 데이터 0, 데이터 1 신호의 비트를 정확히 구분 판독하는 해독 설계입니다.
- **용도**: 리모컨 원격 설정 기능을 탑재하기 위해, 수신 인터럽트와 타이머를 조합하여 표준 NEC 포맷 프로토콜 비트열을 해독하는 데 사용되었습니다.

## 41. 타이밍 마진 완화 및 데드타임 필터링 (Timing Margin Relaxation & Deadtime-Based Filtering)
- **개념**: 고전력 스위칭이 일어날 때 하드웨어 및 소프트웨어 연산 지연으로 인해 미세하게 발생하는 타이밍 오차를 수용할 수 있도록, 상태 전환 앞뒤로 약 수십 마이크로초 단위의 완충 마진(Relaxation)을 가해 오작동을 필터링하는 방식입니다.
- **용도**: PIC MCU의 포트 출력 스위칭 시, 트랜지스터 소자들의 물리적 상승/하강 오차 범위 내에서 스캔 구동 데드타임 타이밍에 마진 딜레이(50us)를 추가 부여하는 데 적용되었습니다.

## 42. 에러 가드 복구 상태 머신 / ERROR_WAIT 상태 (Error-Guard Recovery State Machine / ERROR_WAIT 상태)
- **개념**: 외부 노이즈나 일시적 시스템 불일치로 인해 상태 제어의 무결성이 훼손되었을 때, 시스템을 즉시 강제 리셋하지 않고 가드(Guard) 상태(예: ERROR_WAIT)로 우회 정격 제어하여 시스템에 손상을 주지 않고 안정 상태로 점진 회귀 복구되도록 설계된 상태 머신입니다.
- **용도**: 센서 이상이나 과열 위험 유입 시 강제 치료 중단 및 에러 코드를 표출하며 안전 대기(ERROR_WAIT) 상태로 천이해 안전을 보장하는 로직에 적용되었습니다.

## 43. 타이머 오버플로우 플래그 연동 상태 동기화 (Timer Overflow Flag-Linked State Synchronization)
- **개념**: 소프트웨어 실시간 제어 스레드의 실행 지점을 하드웨어 타이머의 오버플로우 인터럽트 플래그(PIRxbits.TMRxIF) 발생 시점과 완벽히 물리적으로 일치시켜, 타이머 위상과 스캔 상태의 연동 정밀도를 보장하는 제어 동기화 기법입니다.
- **용도**: 디스플레이의 각 위상(DIG1 -> DIG2 -> LED) 스위칭 루틴의 전환 주기를 타이머 플래그 갱신 주기(1ms)에 완벽하게 일치시키는 동기화 제어에 적용되었습니다.

## 44. 16비트 레지스터 버퍼링 동기화 (16-bit Hardware Register Buffering Synchronization)
- **개념**: 8비트 아키텍처의 MCU에서 16비트 레지스터(예: TMR1H/TMR1L)를 다룰 때, 상위 바이트와 하위 바이트를 읽고 쓰는 시점 사이에 카운트 증가가 일어나 값이 심각하게 왜곡되는 것을 막기 위해 하드웨어 래치(Latch) 버퍼링 규칙을 준수하여 동기화하는 기법입니다.
- **용도**: 백그라운드 타이머에 사용되는 Timer1의 16비트 계측 카운트 값을 런타임에 오차 없이 정확히 읽고 쓰기 위해 `TMR1H`를 먼저 읽고 `TMR1L`을 연이어 판독하는 정합 규칙에 적용되었습니다.

## 45. 연속 대기 발광 / Non-Blinking Display (Continuous Standby Illumination / Non-Blinking Display)
- **개념**: FND나 LED의 점멸 위상이 외부 UART 통신 인터럽트나 스위치 상태 판독 속도에 의해 일시 정지되거나 주기가 엉키는 현상을 완전히 없애, 대기 시 화면 깜빡임(Blinking/Flicker)이 전혀 없는 상시 안정 발광을 시각적으로 구현해내는 디스플레이 무중단 제어 기법입니다.
- **용도**: 치료 시작 전 FND 대기 모드에서 상시 안정된 화면 점등 전위를 공급하여 화면 플리커(Flicker) 현상을 방지하는 데 사용되었습니다.

## 46. IDE 정적 분석기 레지스터 인클루드 매핑 (IDE Static Analyzer Register Include Mapping)
- **개념**: VS Code 등 외부 IDE 환경에서 소스 코드 분석 시, XC8 컴파일러용 PIC 레지스터 매크로 상수명들이 정적 분석 도구(clangd 등)에 빨간 줄 에러로 인식되는 문제를 피하기 위해 헤더 인클루드 경로와 정적 레지스터 매크로를 호환 매핑해주는 작업입니다.
- **용도**: 프로젝트 내 `.vscode/c_cpp_properties.json` 파일에 컴파일러 매크로(`__PICC__`, `_16F18855`) 정의를 추가하여 IDE 분석을 무오류 상태로 가동하는 데 사용되었습니다.

## 47. 강제 헤더 인클루드 (Forced Include)
- **개념**: 빌드 시 컴파일러 매개변수나 소스 파일 최상단에 헤더 파일을 강제로 먼저 파싱하게 지시하여, 공통으로 쓰이는 레지스터 정의나 시스템 설정들이 누락 없이 완벽히 빌드 본체에 내장되도록 유도하는 강제 지시 방식입니다.
- **용도**: PIC MCU 헤더 파일인 `<xc.h>`를 소스 파일 `main.c` 및 유틸 모듈 최상단에 빠짐없이 인클루드하여 레지스터 오인식을 완전히 차단하는 데 적용되었습니다.

## 48. 디스플레이 필터링 / FND 출력 억제 (Display Filtering / FND Output Suppression)
- **개념**: 장치가 급격한 설정값 변화 상태에 있거나 시스템 통신 초기화 단계에 있을 때, 비정상적인 쓰레기 데이터나 순간적인 전압 드롭 현상이 디스플레이에 불규칙한 숫자로 표현되는 것을 차단하기 위해 특정 조건에서 디스플레이 출력을 소등 잠금 처리하는 필터링 기법입니다.
- **용도**: 센서 오류 감지 시 혹은 시스템 비상 리셋 시, 안전 복구가 완료될 때까지 일시적으로 공통 애노드 제어 핀을 비활성화(`LATA1 = 1`, `LATA2 = 1` 로 공급 전원 완벽 차단)하여 화면 출력을 안전하게 억제하는 데 사용되었습니다.

## 49. 인터럽트 기반 비블로킹 UART 송신 (Non-blocking UART / Interrupt-driven TX)
- **개념**: CPU가 한 문자씩 시리얼 전송 버퍼가 빌 때까지 대기(Polling)하는 동기식 구조 대신, 송신 전용 버퍼 큐(FIFO Queue)와 송신 인터럽트를 결합하여 데이터를 큐에 밀어 넣기만 하면 하드웨어가 백그라운드에서 한 바이트씩 알아서 전송하게 만드는 넌블로킹 UART 통신 기법입니다.
- **용도**: 디버그 로그 데이터 송신으로 인한 CPU 점유율 증가와 이로 인한 디스플레이 동적 스캔 주기 흔들림 문제를 완벽히 차단하는 데 적용되었습니다.

## 50. 내부 중주파 오실레이터 / MFINTOSC (Medium-Frequency Internal Oscillator)
- **개념**: PIC16F18855 디바이스 내부에 고주파 발진기(HFINTOSC)와 별도로 내장된 독립적인 500kHz의 중주파 정밀 발진 소스입니다.
- **용도**: ADC 변환 시간 마진 확보 및 시스템 저전력 타임 베이스 클럭 공급 소스로 안정적으로 백업 기능으로 사용되었습니다.

## 51. PPS 출력 핀 할당 (Peripheral Pin Select Output)
- **개념**: MCU 내부 디지털 출력 모듈(EUSART1 TX, CCP1 PWM, PWM6 등)의 출력 제어 라인을 실제 포트 C나 B 등의 특정 물리 핀과 결합하기 위해 PPS 출력 매핑 레지스터(RxyPPS)에 할당 부호를 지정해주는 출력 핀 PPS 라우팅 설정입니다.
- **용도**: 부저 구동용 CCP1 출력을 RC0에 할당하기 위해 `RC0PPS = 0x09` 설정을 적용하고, 히터 제어용 PWM6 출력을 RC1에 할당하기 위해 `RC1PPS = 0x0E` 설정을 구성하는 데 적용되었습니다.

## 52. PWM용 타이머 선택 레지스터 (PWM Timer Selection / CCPTMRS)
- **개념**: MCU 내부의 각 CCP 모듈이나 PWM6 기능이 PWM 모듈 구동을 위해 참조할 주기 펄스 제공용 타임 베이스 타이머(Timer2, Timer4, Timer6 등)를 개별 채널별로 선택 지정해주는 전용 하드웨어 제어 레지스터입니다.
- **용도**: CCP1(부저)은 Timer2를 지정하고, PWM6(히터)은 Timer4를 타임 베이스 소스로 지정하여 서로 다른 주파수와 주기로 충돌 없이 구동할 수 있도록 `CCPTMRS0` 및 `CCPTMRS1` 레지스터를 분리 매핑하는 데 사용되었습니다.

## 53. 하드웨어 한계 타이머 리셋 / TxHLT (Hardware Limit Timer Reset / TxHLT)
- **개념**: 타이머 카운팅 중, 물리적인 외부 핀의 에지 신호 입력이나 특정 레지스터 이벤트가 발생하는 순간 하드웨어적으로 타이머 카운터를 자동으로 클리어 및 강제 0점 초기화해주는 정밀 하드웨어 보조 타이머 리셋 기능입니다.
- **용도**: 전력 스위칭 타이밍 제어 및 비상 보호 동작 시, 타이머 카운트를 강제로 외부 이벤트에 동기화해 0점으로 강제 제어하는 안전 장치로 사용되었습니다.

## 54. 리소스 격리 디버깅 (Resource Isolation Debugging)
- **개념**: 시스템 동작 오류의 정확한 하드웨어적 원인을 파악하기 위해, 여러 개의 포트가 공유하고 있는 입출력 전원 배선이나 통신 레지스터 설정을 코드상에서 일시적으로 하나씩 강제 물리/소프트웨어적으로 격리(Isolation)하여 해당 모듈 단독으로 동작을 검증해보는 기법입니다.
- **용도**: 부저가 지속해서 울리는 버그 발생 시, CCP1 매핑과 부저 전원을 강제로 레지스터상에서 꺼두고 히터 스위칭만 단독 가동시켜 전류 및 핀 간섭 여부를 격리 테스트하는 데 사용되었습니다.

## 55. 예약 레지스터 매핑 (Reserved Register Mapping)
- **개념**: 데이터시트 상에 특정 기능의 선택 비트로 정의되지 않고 예약(Reserved)된 설정 값을 실수로 제어 레지스터에 기록하는 경우, MCU가 오동작하여 동작이 멈추거나 출력이 왜곡되는 위험을 방지하기 위해 사용하지 않아야 할 설정 조합을 엄격히 규정하는 맵 설계입니다.
- **용도**: `CCPTMRS0` 레지스터에 타이머를 지정할 때, Reserved 레벨인 `0x00`을 쓰는 대신 올바른 Timer2 설정인 `0x01`을 정확하게 매크로로 매핑하여 타이머 오동작을 미연에 방지했습니다.

## 56. 레지스터 레벨 타이머 라우팅 (Register-level Timer Routing)
- **개념**: 타이머 모듈의 클럭 인풋 소스 및 아웃풋 경로를 단순 I/O 핀이 아닌, 레지스터 설정을 통해 MCU 내부의 다른 하드웨어 모듈(예: PWM, ADCC 트리거 소스 등)에 정교하게 논리적으로 결합하고 방향을 제어하는 라우팅 기법입니다.
- **용도**: Timer4의 완료 신호(Overflow)를 히터 PWM6의 동작 주기로 삼도록 레지스터 레벨에서 클럭 경로를 안전하게 지향(라우팅)하는 제어에 적용되었습니다.

## 57. 중주파 클럭 분주기 (Medium-Frequency Oscillator Divider / MFINTOSC Division)
- **개념**: 내부 중주파 발진기(MFINTOSC)의 원천 500kHz 주파수를 소프트웨어 분주기 설정을 통해 최대 31.25kHz 등 더 낮은 주파수 레벨로 나눔으로써, 동작 클럭 주기를 유연하게 조율하는 기능입니다.
- **용도**: ADC 수렴 시간 확보 및 특수 저주파 계측 클럭을 획득하기 위해 내부 클럭 소스 주파수 비율을 분주하는 제어에 적용되었습니다.

## 58. PPS 출력 코드 검증 및 복구 (PPS Output Code Verification & Restoration)
- **개념**: 런타임 중 정전기나 강한 EMI(전자파) 노이즈 노출로 인해 핀 매핑 레지스터(PPS) 설정 값이 일시적으로 왜곡되어 주변 장치 구동이 실패하는 것을 방지하기 위해, 정기적으로 레지스터의 정상 설정 값을 읽어 검증하고 이상 발견 즉시 원래 코드로 재기록해 시스템 무결성을 자동 복원하는 설계 기법입니다.
- **용도**: 부저 음향 출력 시 RC0 핀의 `RC0PPS` 값이 노이즈로 훼손되어 비프음이 불통되는 오류를 예방하기 위해, 부저 켜기 루틴 진입 시마다 레지스터 값을 `0x09`로 강제 안전 복구해주는 로직에 사용되었습니다.

## 59. CCPTMRS 타이머 비트 오설정 (CCPTMRS Timer Assignment Bit Misconfiguration)
- **개념**: CCP/PWM 모듈이 참조할 동작 타임 베이스 타이머를 지정할 때, 레지스터 비트 오프셋(Bit Offset) 연산 실수를 범해 원래 의도하지 않은 다른 타이머를 중복 바인딩하여 두 모듈의 주파수가 꼬여버리는 심각한 빌드 오류 현상입니다.
- **용도**: 히터 PWM6가 Timer2와 중복 바인딩되어 동작 중 부저 소리와 히터가 꼬이던 버그를 분석하여, 히터의 타이머를 정확하게 Timer4로 할당 분리해 교정 완료한 문제 해결 사례에 적용되었습니다.

## 60. OSCEN 복합 오실레이터 활성화 (OSCEN Composite Oscillator Enabling)
- **개념**: 내부 주 오실레이터 외에, 대기 모드나 보조 클럭으로 사용될 다양한 내부 발진기 소스(HFINTOSC, MFINTOSC, LFINTOSC 등)를 명시적으로 레지스터(`OSCEN`) 설정하여 모두 깨워 활성화해두는 전원 공급 준비 제어 기법입니다.
- **용도**: ADC 정밀 모드 및 시스템 백그라운드 타이머에 전방위 클럭 소스를 지체 없이 교체 공급하도록 내부 발진 소스를 안정적으로 동작 가동해두는 데 적용되었습니다.

## 61. PDF 데이터시트 기반 레지스터 검증 (PDF-Automated Register Bit Verification)
- **개념**: 제조사(Microchip)의 700페이지에 달하는 레지스터 데이터시트 PDF 내용을 기반으로, 제어 레지스터의 개별 비트(Bit) 구조와 16진수 설정 상수의 비트 매칭 정합성을 100% 교차 검증하여 코딩 오차를 사전에 방지하는 펌웨어 검증 표준입니다.
- **용도**: `CCPTMRS0` 레지스터의 bit[1:0]인 `C1TSEL`과 `CCPTMRS1` 레지스터의 bit[3:2]인 `P6TSEL`의 타이머 매핑 헥사 값의 정합성을 교차 검증하여 코드를 완벽 복구하는 데 적용되었습니다.

## 62. GPIO 수동 제어 디버깅 (GPIO Manual Control Debugging)
- **개념**: 하드웨어 주변 모듈(CCP 등)을 일시 정지시키고, 디버그 코드를 통해 특정 GPIO 핀의 TRIS 및 LAT 레지스터 값을 강제로 수동 제어(`0` 과 `1` 반복 인가)하여 연결된 부하 장치의 전기적 스위칭 상태를 실시간으로 직접 확인하는 하드웨어 진단 기법입니다.
- **용도**: 부저 드라이버 회로의 동작 전위 마진 및 TR의 전류 정합 상태를 체크하기 위해 `LATC0 = 1` 과 `LATC0 = 0` 신호를 직접 수동으로 반복 줘가며 회로의 구동 신뢰성을 진단하는 데 적용되었습니다.

## 63. UART CLI 디버깅 (UART CLI Debugging)
- **개념**: 시리얼 포트(EUSART)를 통해 시스템 개발 중 내부 주요 변수, ADC 센서 전압값 등을 화면에 출력하고, 런타임 제어 키 명령을 즉석 입력받아 특정 소프트웨어 함수들을 화면 상에서 즉시 강제 테스트 및 실행 교정할 수 있게 지원하는 UART 명령줄 인터페이스 디버깅 기법입니다.
- **용도**: 치료 강도 선택, 센서 값 체크, 부저 수동 작동 등 메인 보드 상의 전체 물리 태스크를 PC에서 가상의 터미널 창을 띄워 확인 및 진단하는 데 사용되었습니다.

## 64. 하드웨어 격리 측정 (Hardware Isolation Measurement)
- **개념**: 서로 다른 장치가 하나의 전원 레일이나 신호 라인을 물리적으로 공유하고 있을 때, 특정 부하 작동 시 다른 장치에 전기적 강하(Drop)나 잡음 전위가 얼마나 전이되는지 특정 포트를 격리 조치한 상태에서 전위 변화를 물리적으로 직접 측정해 확인하는 설계 디바이스 진단법입니다.
- **용도**: 부저 비프 동작이 실행 중인 히터 PWM의 스위칭 리플이나 전압 강하에 어떤 상관관계를 미치는지 회로를 격리 분석해 안전성을 점검하는 데 적용되었습니다.

## 65. 레지스터 덤프 교차 검증 (Register Dump Cross-Verification)
- **개념**: 프로그램 구동 중의 실제 I/O 상태와 MCU 하드웨어 레지스터 상태가 완벽히 일치하는지 규명하기 위해, 런타임에 MCU 내부의 특정 하드웨어 주변장치 제어 레지스터 전체 데이터를 UART를 통해 원시 16진수 바이트 값으로 고스란히 덤프 출력하여 데이터시트 스펙과 한 줄씩 매칭해 보는 정합성 교차 정밀 진단법입니다.
- **용도**: Timer2, CCP1CON, T2CON, PR2 등 부저 제어와 관련된 4개 이상의 핵심 특수기능 레지스터(SFR) 덤프를 출력하여 소프트웨어 초기화 값과 일치하는지 대조 분석하는 데 사용되었습니다.

## 66. 그라운드 쇼트 고장 (Short-to-GND Failure)
- **개념**: 신호 배선이나 특정 I/O 포트 단자가 불완전한 땜 납땜 불량이나 물리적 손상으로 인해 그라운드(GND/0V) 전위 전선과 영구적으로 쇼트(합선)되어, 출력 전압을 아무리 인가해도 무조건 0V LOW 상태로 핀이 고정되어 버리는 물리적 고장 형태입니다.
- **용도**: 부저 구동 포트(`RC0`) 및 외부 스위칭 트랜지스터(Q5)의 베이스 단자가 전선 마찰이나 기판 불량으로 그라운드에 쇼트되어 부저가 불통되는 고장이 하드웨어 상에 발생하지 않았는지 전류 누설 검사를 진행하는 분석 기준이 되었습니다.

## 67. 포트 C 오픈드레인 제어 레지스터 (ODCONC)
- **개념**: PIC16F18855의 포트 C에 적용된 오픈드레인(Open-Drain) 동작 방식 활성화 제어 레지스터로, 해당 비트값을 `1`로 주면 핀이 LOW(0)일 때는 0V 그라운드 레벨을 적극 구동하고 HIGH(1)일 때는 오픈(하이 임피던스/플로팅) 상태가 되도록 제어하는 레지스터입니다.
- **용도**: 부저 구동 포트 `RC0`를 완전한 0V/5V 구동(Push-Pull) 상태로 만들기 위해 오픈드레인 활성 레지스터 `ODCONC`의 0번 비트를 완벽하게 0(`ODCONCbits.ODCC0 = 0`)으로 제어하여 소리 파형을 확실히 보정하는 데 사용되었습니다.

## 68. 포트 C 슬루레이트 제어 레지스터 (SLRCONC)
- **개념**: 포트 C의 출력 신호 전위가 LOW에서 HIGH로, 혹은 HIGH에서 LOW로 변경될 때 전위가 상승/하강하는 속도인 슬루레이트(Slew Rate)를 정격 한도로 제한하여 급격한 상태 전이 시 발생하는 스파이크 노이즈와 EMI(고주파 노이즈) 방출을 소프트웨어적으로 억제하는 레지스터입니다.
- **용도**: 부저 출력 포트 `RC0`의 스위칭 주파수가 급변 시 인접 아날로그 입력선(AN0/풋 센서)에 고주파 간섭을 주는 문제를 차단하기 위해, 슬루레이트 억제 기능(`SLRCONCbits.SLRC0 = 0`)을 완벽하게 오프하거나 전위 변화율을 안정 궤도로 동기화하는 데 사용되었습니다.

## 69. 하이 임피던스 / 플로팅 상태 (High Impedance / Floating State)
- **개념**: 물리적으로 핀이 그라운드(0V)와도 전원(3.3V/5V)과도 완전히 절단된 전기적 공중분할 상태가 되어, 전선 주변의 전자기 유도 잡음이나 정전기적 자극에 따라 입력 핀의 상태가 HIGH와 LOW를 불안정하게 오가며 진동하게 만드는 전기적 핀 상태입니다.
- **용도**: 입출력 방향 전환 시 핀이 플로팅되어 고속 스위칭 잔상이 오동작을 일으키거나, FND 자릿수 포트가 하이 임피던스로 방치되어 이전 전하 잔재가 고스트 현상을 일으키지 않도록 WPU 제어 및 소등 LAT 제어를 엄밀화하는 데 적용되었습니다.

## 70. 냉납 (Cold Joint / Cold Solder Joint)
- **개념**: PCB 제작 시 땜납의 가열 온도가 낮았거나 납이 굳기 직전에 스위치 부품이 미세하게 진동하여, 외형상으로는 납땜이 붙어있어 보이지만 실제 내부적으로는 접점이 완전 결합되지 않아 접촉 압력이나 주변 온도에 따라 신호가 단락되는 불량 현상입니다.
- **용도**: 시간 UP/DOWN 키를 물리적으로 강하게 누를 때만 동작하거나 평상시 키 상태 전위가 불완전하게 유입되는 원인을 진단할 때, 냉납에 의한 하드웨어 접촉 결함을 스캔 필터 및 전기적 도통 검사를 통해 규명하는 진단 기준으로 쓰였습니다.

## 71. 2중 병렬 구동 및 출력 라우팅 (Twin Parallel Drive / Dual Output Routing)
- **개념**: 단일 포트가 공급할 수 있는 출력 허용 전류(최대 25mA) 한계를 넘는 고전력 소자(예: 치료용 강전 전력 히터)를 제어하기 위해, MCU의 동일 신호 출력 매핑을 가진 두 개의 포트 핀을 병렬 연결하여 하드웨어적 출력 허용 전류 마진을 2배로 확장시켜 구동하는 출력 병렬화 설계입니다.
- **용도**: 히터 PWM 출력을 RC1 핀 외에 보조 스위칭 드라이버 핀에 동시에 병렬 PPS 할당해 스위칭 소자의 동작 상승 마진을 정격 보장하는 데 적용되었습니다.

## 72. 점퍼 와이어 우회 수리 (Jumper Wire Bypass Repair)
- **개념**: 기판 내부 동박 패턴이 정전기 서지 충격으로 타거나 물리적으로 단선(Open)되어 신호 전달이 끊어졌을 때, 기판 겉면에 직접 얇은 피복 절연선을 납땜으로 결합해 원래의 전기적 정합 경로로 신호를 우회 연결하여 정상화해 내는 물리 수리 기법입니다.
- **용도**: PCB 패턴 손상으로 인해 부저 RC0 출력이 기판에서 끊겨 신호 전달이 안 될 때, 피에조 구동 트랜지스터 베이스로 직접 점퍼 와이어를 연결해 신호를 바이패스하는 회로 수리 방침의 기준이 되었습니다.

## 73. 인덕티브 킥백 서지 데미지 및 인접 핀 간섭 (Inductive Kickback Surge Damage / Adjacent Pin Destructive Interference)
- **개념**: 모터, 코일, 히터 등 유도성 인덕터 부하가 동작을 멈출 때, 내부에 축적되어 있던 자기장 에너지가 역기전력(Kickback) 형태로 변환되어 스위칭 차단 시점에 순간 수백V에 달하는 고전압 서지를 발생시켜 회로를 태우거나 인접 핀에 유도 노이즈를 강하게 퍼뜨리는 하드웨어 파괴 현상입니다.
- **용도**: 1MHz 고주파 히터 스위칭 시 발생하는 고전력 유도 서지가 인접한 풋 센서 아날로그 전압선(`AN0`)에 침범하여 수 볼트 단위의 글리치 파형을 발생시키는 하드웨어 간섭 기하학적 분석 모델로 적용되었습니다.

## 74. GPIO 핀의 타이머 외부 클럭 오버라이드 (Timer External Clock Override on GPIO / T4CKI Override)
- **개념**: 특정 GPIO 핀(T4CKI 등)의 물리 전압 입력 상태를 타이머 모듈의 외부 카운트 베이스 클럭 소스로 오버라이드 지정하는 특수 기능입니다.
- **용도**: Timer4의 소스 입력이 엉뚱한 외부 물리 입력 핀과 잘못 연동되어 멈추는 오작동을 제거하기 위해, Timer4 클럭 소스를 순수하게 내부 Fosc/4 클럭으로 고정 레지스터 바인딩하는 제어에 적용되었습니다.

## 75. 데이터 신호 변조기 / DSM (Data Signal Modulator)
- **개념**: 하나의 반송파(Carrier) 신호(예: 고주파 PWM)를 다른 데이터 신호(예: UART TX 또는 저주파 변조 신호)와 결합 및 합성(Modulation)하여 하나의 최종 혼합 변조 파형을 출력 핀에 자동으로 뿜어내 주는 MCU 내부 하드웨어 변조 모듈입니다.
- **용도**: 히터 전력 파형에 미세 고주파 노이즈 성분을 합성하여 치료 효율을 높이거나, 광대역 스위칭 변조 신호를 핀에 출력하기 위해 하드웨어 변조기로 적용되었습니다.

## 76. 버스트 모드 고주파 변조 PWM (Burst Mode High-Frequency Modulated PWM)
- **개념**: 히터 출력을 전력 제어할 때 상시 듀티로 켜두지 않고, 수십kHz 단위의 고주파 PWM 파형을 특정 주파수로 켜고 끄는 버스트(Burst) 구동을 합성 적용하여 물리적인 과열을 방지하면서 정밀 에너지를 공급하는 변조 기법입니다.
- **용도**: 출력 과도 현상을 제어하기 위해, PWM6 출력 위상에 주기적인 소등 버스트 인터벌을 소프트웨어 및 DSM을 활용해 조율하는 로직에 적용되었습니다.

## 77. 타이머 연동 OOK 변조 (Timer-Linked On-Off Keying Modulation)
- **개념**: 반송파 신호의 출력을 디지털 1과 0 데이터 비트 전위에 동기화하여 출력 있음(On)과 출력 없음(Off) 상태의 두 위상으로 번갈아 스위칭 변조하는 키잉(Keying) 변조 제어입니다.
- **용도**: 부저의 특정 멜로디 음향 비프 듀티를 온/오프하는 비프 인터벌 타이밍 제어에 적용되었습니다.

## 80. TRIS 레지스터를 활용한 물리 핀 잠금 보호 (Physical Pin Lock Protection - TRIS Direct Control)
- **개념**: 주변 모듈(PWM, EUSART 등)이 활성화되어 핀 출력을 지배하고 있더라도, GPIO 방향 레지스터(TRIS) 값을 강제로 `1`(INPUT/하이 임피던스)로 전환하여 핀의 물리적 출력을 완전히 물리 차단(Lock)하고 부하 구동 전류를 100% 잠그는 강제 안전 잠금 기법입니다.
- **용도**: 센서 과열이나 부저 잔존 음 발생 등 긴급 셧다운 상황 발생 시, 단순히 PPS를 끄는 것보다 안전하게 TRIS 레지스터 값을 직접 제어하여 포트 물리 출력을 안전한 입력 플로팅 전위로 묶어 두는 비상 록아웃 로직에 적용되었습니다.

## 81. 타이머 클럭 안전 분주 (Timer Clock Safe Division)
- **개념**: 클럭 주파수가 너무 빠를 때 타이머 레지스터의 오버플로우가 빈번하게 일어나 연산 인터럽트 부하가 폭증하는 것을 막기 위해, 전분주기(Prescaler) 및 후분주기(Postscaler) 값을 안전 정격 분주 비로 낮추어 시스템이 안정 동작 가능한 최적의 인터럽트 주기를 형성해주는 설계입니다.
- **용도**: Timer2 및 Timer4의 프리스케일러 설정을 각각 `1:8` 및 `1:16` 등으로 안전 정격화하여, 인터럽트 오버헤드 없이 정확한 1ms 동작 계측 타임을 추출하는 데 적용되었습니다.

## 82. 실시간 런타임 락 라우팅 (Real-time Runtime Lock Routing)
- **개념**: 런타임 중에 중요한 제어 레지스터 설정값(예: PPS 매핑 등)이 변경되는 위험을 원천 차단하기 위해, 레지스터 쓰기 잠금 지시(`PPSLOCK = 0x55/0xAA`)를 통해 런타임 잠금을 동적으로 해제 후 재라우팅한 뒤 즉시 다시 잠가 런타임 안전성을 지키는 제어 안전 기법입니다.
- **용도**: 부저 구동 및 통신 초기화 시 PPS 핀 매핑 경로를 설정할 때, `PPSLOCK` 해제 시퀀스를 정확히 밟고 매핑 후 즉시 재잠금 처리하는 데 적용되었습니다.

## 83. 계산 기능 내장 ADC / ADCC (Analog-to-Digital Converter with Computation / ADCC)
- **개념**: 단순 아날로그 전압의 디지털 변환을 넘어, 하드웨어 연산 장치가 자체 내장되어 CPU 개입 없이 아날로그 샘플링 값의 필터링, 누적, 평균값 계산 및 문턱 레벨 감시를 하드웨어적으로 자율 수행해주는 지능형 ADC 모듈입니다.
- **용도**: 풋 센서(`FOOT_SEN`, `AN0`) 전압의 실시간 아날로그 리딩 감시 속도를 하드웨어 단에서 자동 스케일링하여 CPU 연산 부하를 억제하는 데 사용되었습니다.

## 84. ADC 전용 고속 RC 발진기 클럭 / FRC (Fast RC Oscillator for ADC / FRC)
- **개념**: MCU가 슬립(Sleep) 모드에 진입하거나 주 시스템 클럭 주파수가 변동하더라도 이에 영향받지 않고, 아날로그-디지털 변환 연산의 정확성을 일정한 시간 주기로 보장해주기 위해 내장된 ADC 전용 독립형 내부 고속 RC 발진기 클럭 소스입니다.
- **용도**: ADC 수렴 시간(Acquisition Time) 계측 오차를 차단하기 위해 ADC 변환 클럭 소스를 `FRC` 클럭으로 고정 설정하여 아날로그 측정 신뢰도를 최대화하는 데 사용되었습니다.

## 85. 선형 비례 보간 피드백 제어 (Linear Interpolation Feedback Control)
- **개념**: 입력값(아날로그 풋 센서 전압)의 변화 범위(예: $0\text{V} \sim V_{\text{threshold}}$)를 설계된 출력값의 변화 범위(예: $500\text{ }\mu\text{s} \sim \text{max\_duration\_us}$)에 실시간으로 매핑하여, 외부 자극(접촉 정도)의 물리적 세기에 비례하도록 시스템 출력을 부드럽고 조화롭게 선형 가변시키는 닫힌 루프(Closed-loop) 형태의 피드백 제어 기법입니다.
- **용도**: 심부발열기 히터 동작 중 부하(손/발)가 출력부에 접촉함에 따라 상승하는 풋 센서(`FOOT_SEN`, `AN0`)의 ADC 계측값을 추종하여, 저주파 PWM7의 지속시간을 기본 최소치인 $500\text{ }\mu\text{s}$부터 해당 동작 레벨의 최대 허용 지속시간까지 정밀하게 선형 보간하여 히터 출력을 접촉 정합도에 연동 가변하는 데 핵심 제어 로직으로 적용되었습니다.

## 86. 동적 문턱값 클램핑 / 포화 제어 (Dynamic Threshold Clamping / Saturation)
- **개념**: 실시간 피드백 제어 중 입력 센서 값이 설계된 상한 문턱값(Upper Threshold)을 초과하거나 하한 문턱값보다 낮아지는 입력 예외 상황이 발생했을 때, 출력 연산 결과가 최대 허용 범위를 넘어서 발산(Overflow)하지 않도록 최대 한계값(Max Clamping Limit)이나 최소 한계값으로 강제 고정 및 억제하는 포화 제어 기법입니다.
- **용도**: 풋 센서 ADC 입력값(`FOOT_SEN`)이 설계된 문턱 전압 임계값(`FOOT_ADC_THRES_HI`)을 돌파할 경우, PWM7 지속시간이 최대치인 `max_duration_us`를 넘어 오버플로우되어 오작동하지 않도록 강제로 최댓값으로 고정 클램핑하여 장비 안전성과 정밀 출력을 보장하는 데 적용되었습니다.

## 87. 오버샘플링 및 평균화 필터 / Software LPF (Oversampling and Averaging Filter / Software LPF)
- **개념**: 아날로그 센서 값을 1회 계측하여 즉시 제어에 반영하지 않고, 극히 짧은 시간 간격으로 N회(예: 16회) 연속 샘플링(Oversampling)하여 누적한 뒤 평균값을 구함으로써 신호에 인입된 일시적인 고주파 노이즈 스파이크 성분과 랜덤 임펄스 노이즈를 완벽하게 상쇄 감쇄시켜 주는 소프트웨어적인 로우 패스 필터(Low Pass Filter) 설계 패턴입니다.
- **용도**: 1MHz에 달하는 히터의 강한 스위칭 전력 스파이크가 풋 센서 아날로그 전압선(`AN0`)에 인입되어 ADC 변환값이 튀는 간섭 현상을 차단하기 위해, `ADC_Read(0)` 호출 시 실시간으로 16회 오버샘플링하여 합산 평균값을 구함으로써 센서 전압의 정밀한 정상 상태(DC Level) 전위만을 안전하게 추출하는 데 사용되었습니다.

## 88. 1초 단계별 피드백 제어 (1-Second Stepwise Feedback Control)
- **개념**: 센서 입력 변화에 대응하는 피드백 제어 주기를 밀리초 단위로 너무 빠르게 가변하면 전력 구동 소자가 급격하게 출력을 흔들어 수명 드롭이나 펌핑 노이즈를 유발하므로, 제어의 변화 가중치 및 피드백 계산 실행 타임을 매 1초(백그라운드 소프트웨어 타이머 만료 주기) 간격으로 분할 조율하여 안정적으로 출력을 보정해나가는 위상 비례 제어 방식입니다.
- **용도**: 풋 센서 접촉 정합도에 따른 히터 가변 PWM7 갱신을 1초 소프트웨어 타이머인 `tick_1s` 발생 타이밍에만 정밀 동기화하여 갱신함으로써, 불필요한 급격한 전력 충격을 예방하고 사용자에게 균일한 열량 상승감을 제공하도록 제어 주기를 조정한 데 적용되었습니다.

## 89. 비동기식 부저 구동 제어 (Asynchronous Buzzer Control)
- **개념**: 메인 제어 루프의 흐름을 방해(Blocking)하는 동기식 딜레이 함수(`__delay_ms`)를 사용하지 않고, 배경 동적 스캔 주기를 기준으로 소프트웨어 타이머 카운트를 누적 감시하여 설정된 비프음 출력 시간(예: 100ms)이 경과하면 비동기적으로 부저 구동 포트(`RC0`) 및 CCP1 PWM 출력을 안전하게 자동 소등 및 차단하는 넌블로킹(Non-blocking) 형태의 소프트웨어 구동 기법입니다.
- **용도**: 키 입력 신호 감지 시 디스플레이 dynamic scanning 스캔 흐름(2ms 주기)을 무중단 유지하면서 사용자 키 비프 피드백 음을 깔끔하게 출력하기 위해 적용되었습니다.

## 90. 하드웨어 한계 타이머 잔재 초기화 (Hardware Limit Timer Reset / HLT Reset)
- **개념**: 외부 리셋 신호(RST)나 강제 전원 차단 등의 이벤트 발생 시, 하드웨어 타이머(TMR2, TMR4, TMR6) 제어 레지스터 및 하드웨어 한계 제어(HLT) 레지스터 내부에 남아 있을 수 있는 불완전한 상태 값과 잔재 카운트를 초기화하여 예기치 않은 타이머 인터럽트 오동작이나 PWM 출력 멈춤 문제를 예방하는 리셋 제어 로직입니다.
- **용도**: 심부발열기 디바이스 부팅 및 시스템 리셋 진입 시 타이머2/4/6의 잔재 주기를 완전히 클리어하고 PWM 모듈과의 PPS 매핑 경로를 오작동 없이 초기화하는 데 적용되었습니다.

## 91. 환경 변수 경로 불일치 대응 및 XC8 크로스 컴파일 빌드 (Environment Path Mismatch Resolution & XC8 Cross-Compilation)
- **개념**: 호스트 시스템의 환경 변수(PATH)가 참조하는 소프트웨어 디렉토리 경로와 실제 도구(예: XC8 컴파일러)가 설치된 물리적인 드라이브/폴더 구조가 상이할 때 발생하는 빌드 에러를 진단하고, 실제 설치 경로를 추적하여 컴파일러 바이너리를 직접 지정해 타겟 시스템(PIC16F18855 MCU)에서 구동될 무결한 실행 파일(hex 펌웨어)을 컴파일 및 빌드해내는 일련의 펌웨어 빌드 프로세스입니다.
- **용도**: MCU 펌웨어 개발 작업 시 윈도우 PATH 에러로 인해 `xc8.exe` 크로스 컴파일러 호출이 실패하는 오류를 해결하기 위해, D 드라이브에 설치된 실제 경로인 `D:\Program files\Microchip\xc8\v2.45\bin\xc8.exe`를 발굴해 내어 최신 `main.c` 소스 코드를 성공적으로 빌드해 `main.hex`를 생성 및 갱신 배포하는 데 적용되었습니다.

## 92. 동적 PPS 격리 및 하드웨어 가드 차단 (Dynamic PPS Isolation & Hardware-Level Guard Shutoff)
- **개념**: 하드웨어 주변장치 모듈(예: CCP/PWM, Timer 등)의 동작 레지스터를 꺼두어도(`CCP1CON = 0x00`), 핀에 묶인 PPS 출력 매핑 정보(`RC0PPS = 0x09`)가 지속 유지될 경우 잔류 클럭이나 임베디드 실리콘 잠금 결함에 의해 출력 핀에 비정상적인 전압 전위가 무한히 고착되는 현상을 막기 위해, 소등 시점 즉시 핀 매핑 레지스터 자체를 일반 GPIO 상태(`0x00`)로 환원하여 물리적인 차단 선로를 형성하는 고 신뢰성 펌웨어 차단 설계 기법입니다.
- **용도**: 비동기 부저 타이머 만료 및 감쇄 연주 완료 즉시 부저 구동용 물리 핀 `RC0`의 PPS 매핑 설정을 완전히 해제(`RC0PPS = 0x00`)함으로써, 부저 모듈과 핀 간의 논리 선로를 완전 분리해 부저가 계속 울리는 무한 삑소리 결함을 100% 원천 차단하고 즉시 0V로 정지시키는 로직에 적용되었습니다.

## 93. 삼중음 키 재트리거 차단 필터 (Triple-Tone Key Re-trigger Block Filter)
- **개념**: 시스템이 여러 음계(낮은음, 중간음, 높은음)로 구성된 순차적인 멜로디 시퀀스(예: 스타트 상승 삼중음 또는 스톱 하강 삼중음)를 비동기식 소프트웨어 타이머를 통해 재생하고 있는 동안, 사용자의 버튼 조작이나 적외선 리모컨 키 신호가 추가로 유입되었을 때 부저 타이머나 모드 변수가 리트리거(재초기화)되어 멜로디의 템포가 끊기거나 소리가 꼬이는 것을 구조적으로 차단하는 소프트웨어 오동작 방지 필터 기법입니다.
- **용도**: 물리 키 및 리모컨에서 부저가 삼중음(mode=1, 2)으로 구동 중일 때(`buzzer_timer > 0 && (buzzer_mode == 1 || buzzer_mode == 2)`), 부저 구동 레지스터를 재설정하거나 타이머 값을 덮어쓰는 기능을 차단함으로써 멜로디가 꼬임 없이 끝까지 안정적으로 울려 퍼지도록 보장하는 데 적용되었습니다.

## 94. 모듈러 아키텍처 및 소스 분할 리팩토링 (Modular Architecture & Source-Splitting Refactoring)
- **개념**: 하나의 비대하게 응집된 소스 파일(Single Giant Source File) 내부의 서브 루틴들을 기능적 성격(입력 제어, 디스플레이 출력, 하드웨어 PWM 구동, 통신 프로토콜 디코딩 등)에 따라 독립된 물리 파일들(`.c` 및 헤더 `.h`)로 파편 분리하여 코드 간 결합도(Coupling)를 낮추고 독립성과 유지보수 무결성을 확보하는 구조적 리팩토링 기법입니다.
- **용도**: 약 1500라인에 육박하던 단일 `main.c` 소스를 역할에 따라 `main.c/h`, `system.c/h`, `display.c/h`, `key.c/h`, `pwm_control.c/h`, `remote.c/h`로 깔끔하게 물리 분리하여 펌웨어의 관리 편의성을 극대화한 아키텍처 설계에 적용되었습니다.

## 95. extern 지시자를 활용한 다중 모듈 데이터 바인딩 (Multi-Module Data Binding via extern Directive)
- **개념**: 여러 개의 분할 컴파일 단위(Translation Unit) 파일들 간에 공통 상태 플래그나 제어 데이터 변수들을 참조 및 갱신할 수 있도록, 헤더 파일에 변수 타입을 `extern` 키워드로 선언하고 단 하나의 목적 C 파일 내부에 실제 변수 공간을 실질적으로 확보(정의)하여 컴파일 및 링커 시점에 동일한 변수 주소로 안전하게 연결(바인딩)되도록 지시하는 C 언어 연동 기법입니다.
- **용도**: 각 모듈(`key.c`, `display.c`, `pwm_control.c` 등)에서 실시간으로 공유하고 동작 판정해야 하는 런타임 제어 전역 변수들(`setting_mode`, `is_running`, `current_level`, `buzzer_timer` 등)을 다중 컴파일 환경에서 안전하게 상호 링크 및 공유하여 사용하도록 구성하는 데 적용되었습니다.


