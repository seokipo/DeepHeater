# 심부발열기 디스플레이 보드 제어 개발 일지 (DEVELOPMENT_LOG.md)

본 문서에는 심부발열기 프로젝트의 버전별 주요 개발 및 변경 사항을 기록합니다.

---

## 2026-06-12 (V0.9.66)
- **버전**: V0.9.66
- **주요 변경사항**: XC8 컴파일러 빌드 부산물(`.obj`, `.cmf`, `.rlf`)의 Git 추적 제외를 위한 `.gitignore` 설정 정비 및 Git 형상 관리 정리, 그리고 GitHub 저장소 원격 업로드 완료.
  - **진단**: 컴파일러 빌드 과정에서 대량으로 생성되는 중간 빌드 아티팩트 파일(`.obj`, `.cmf`, `.rlf`)들이 Git 트래킹 대상에 포함되어 형상 관리 히스토리가 불필요하게 커지고 더러워지는 문제를 확인.
  - **조치**: 
    1. `.gitignore` 파일에 `*.obj`, `*.cmf`, `*.rlf` 패턴을 추가함.
    2. 이미 트래킹되고 있던 `main.obj`, `main.cmf`, `startup.rlf` 파일들을 `git rm --cached` 명령을 사용하여 Git 추적 목록에서 물리적으로 안전하게 소거함.
  - **결과**: 빌드 시 생성되는 임시 부산물들이 변경 감지 대상에서 완전히 제외되어 깨끗한 형상 관리 체계(소스 코드 및 배포 바이너리 `main.hex` 단독 타게팅)를 구축하였으며, 원격 저장소(`https://github.com/seokipo/DeepHeater`)에 최종 커밋 및 Push를 완료함.

---

## 2026-05-27 (V0.9.65)
- **버전**: V0.9.65
- **주요 변경사항**: EEPROM PWM 세팅 설정값 가변 범위의 임계 하한선 확장 갱신 (20 -> 15) 완료.
  - **진단**: 사용자의 요청에 의해, 설정 모드에서 조작 가능한 PWM 최소 범위(하한 임계점)를 기존 `20` 에서 `15` 로 넓혀 미세 기동 능력을 확보하고자 함. (최종 조절 범위는 `15 ~ 55` 가 됨)
  - **조치**: 
    1. `main.c` 의 EEPROM 부팅 로딩 복구 조건식을 `pwm_setting_value < 20` 에서 `pwm_setting_value < 15` 로 하한을 낮춤.
    2. `remote.c` 내 `IR_Process_Command()` 의 설정값 감소 조건 하한선을 `pwm_setting_value > 20` 에서 `pwm_setting_value > 15` 로 확장해 리모컨 1번 버튼(감소) 조작 시 15 레벨까지 도달하도록 제어 범위를 보정함.
  - **결과**: 다중 크로스 컴파일 성공 및 최신 실행 파일(`main.hex`) 빌드 완료. (Program Space: 15E0h, 68.4% 사용). 설정 모드에서 리모컨 1번 버튼(감소) 조작 시 FND 출력 값이 15까지 원활하게 감소하며 복구 시에도 15 범위 내에서 정상 회복됨을 최종 검증.

---

## 2026-05-27 (V0.9.64)
- **버전**: V0.9.64
- **주요 변경사항**: EEPROM PWM 세팅 설정값 가변 범위의 임계 상한선 축소 갱신 (65 -> 55) 완료.
  - **진단**: 사용자의 기기 구동 제어 편의 및 히터 전력 안정성을 위해, EEPROM 설정모드에서 조율할 수 있는 PWM 설정 범위를 기존 `20 ~ 65` 에서 `20 ~ 55` 로 안전 상상 한계를 축소 제한해 달라는 요청을 반영함.
  - **조치**: 
    1. `main.c` 의 EEPROM 부팅 로딩 복구 조건식을 `pwm_setting_value > 65` 에서 `pwm_setting_value > 55` 로 축소함.
    2. `remote.c` 내 `IR_Process_Command()` 의 설정값 증가 조건 상한선을 `pwm_setting_value < 65` 에서 `pwm_setting_value < 55` 로 조율하여 리모컨으로 값을 조작할 때의 최종 출력 한계점을 55로 격리함.
  - **결과**: 다중 크로스 컴파일 성공 및 최신 실행 파일(`main.hex`) 빌드 완료. (Program Space: 15E0h, 68.4% 사용). 설정 모드에서 리모컨 2번 버튼(증가) 조작 시 FND 출력 값이 55를 초과하지 않고 정지하며, EEPROM 복구 시에도 55 범위 내에서 정상 동작함을 검증.

---

## 2026-05-27 (V0.9.63)
- **버전**: V0.9.63
- **주요 변경사항**: 설정 모드 동작 하에서 물리적 키 스위치 제어 차단(마스킹) 및 오직 적외선 리모컨 단독 조작 지원 구조 구현 완료.
  - **진단**: 사용자의 요청에 의해, 부팅 시 설정 모드(45)로 진입할 때만 물리 키(SW1+SW2)를 사용하고, 일단 진입에 성공한 이후의 세부 설정 단계(값 증감 및 저장)에서는 PCB 기판 상의 물리 버튼들(SW3, SW4 등)이 오동작하거나 임의 변조되는 것을 예방하기 위해 조작 입력을 차단하고 오직 리모컨으로만 사용 가능하도록 제어 권한 분리가 필요함을 인지함.
  - **조치**: 
    1. `key.c` 내 `Key_Process()` 함수에서 설정 모드 활성(`setting_mode == 1`) 판정 조건문 내부의 구체적인 값 증감(SW3~SW6) 및 저장(SW1/SW2) 관련 연산 코드를 전면 격리 제거함.
    2. 대신, 해당 분기 진입 시 무조건 `continue;` 하도록 제어 시퀀스를 변경하여, 설정 모드 동작 중에는 PCB 기판 버튼 입력이 완전히 비활성 마스킹 처리되도록 정비함.
    3. 적외선 리모컨 해독 분기(`remote.c` 의 `IR_Process_Command()`) 내부의 설정 제어 동작은 그대로 유지하여, 오직 리모컨 명령어만으로 듀티비 가변 및 EEPROM 저장이 가동되도록 설계를 단원화함.
  - **결과**: 다중 크로스 컴파일 성공 및 최신 실행 파일(`main.hex`) 빌드 완료. (물리 키 처리부 삭감 최적화에 의해 Program Space 점유율이 70.8%에서 **68.4%**로 추가 2.4% 메모리 대폭 감축 혜택 확보). 설정 모드 기동 시 물리 버튼은 전혀 반응하지 않으며, 오직 무선 리모컨을 통해서만 값 조율 및 저장이 무결하게 제어됨을 확인.

---

## 2026-05-27 (V0.9.62)
- **버전**: V0.9.62
- **주요 변경사항**: 설정 모드 부팅 진입 직후의 강제 탈출 오류(Key-overlap) 방지를 위한 키 릴리즈 대기 가드(Key Release Guard) 구현 및 정상 진입 완료.
  - **진단**: 부팅 시 설정 모드(45)로 진입하자마자 사용자가 누르고 있던 진입용 스위치(SW1/SW2)에서 미처 손을 떼기 전에 메인 넌블로킹 스레드 루프(`while(1)`)가 초고속으로 회전하기 시작하여, 단 30ms 만에 `Key_Process()` 가 "SW1 또는 SW2가 눌림" 상태로 판단해 즉시 EEPROM 저장을 실행하고 설정 모드를 원복(`setting_mode = 0`)시켜 버렸던 논리 버그를 포착함. 이로 인해 부저음만 진입한 것처럼 삑- 하고 나고 FND 화면에는 설정 모드가 보이지 않은 채 대기 화면(30)이 즉시 나오는 오동작을 규명함.
  - **조치**: 
    1. `main.c` 의 설정 모드 진입 성공 처리부 내부에, 사용자가 두 진입용 버튼에서 완전히 손을 떼어 포트 B의 신호가 모두 HIGH(`PORTB & 0x03 == 0x03`) 상태로 회귀할 때까지 루프 대기시키는 **`Key Release Guard`** 소스 코드를 추가함.
  - **결과**: 다중 크로스 컴파일 성공 및 최신 실행 파일(`main.hex`) 빌드 완료. (Program Space: 16A8h, 70.8% 사용). 전원을 켤 때 SW1과 SW2 버튼을 누르고 있다가 삑- 하는 진입 비프음이 들릴 때 손을 떼면, 설정 값(45)이 FND에 선명하게 유지되며 실제 물리 버튼 및 리모컨으로 설정 기능이 완벽하게 가동됨을 검증함.

---

## 2026-05-27 (V0.9.61)
- **버전**: V0.9.61
- **주요 변경사항**: 부팅 시 설정 모드(EEPROM 값 변경) 진입용 SW1+SW2 판독의 방전 시정수 대기 지연 보강을 통해 진입 불통 결함 완벽 해결.
  - **진단**: 
    1. 전원 인가 및 리셋 직후, 포트 B가 강력한 디지털 출력 상태(`LATB=0xFF`)로 구동되고 있다가 즉시 입력 방향(`TRISB=0x3F`)으로 변경하여 키 눌림을 감지할 때, 기존의 대기 딜레이인 `10us`가 너무 짧아 회로 라인 및 기생 용량에 충전된 전하가 스위치를 통해 0V 논리 LOW 레벨로 방전될 물리적 시간이 부족했음을 규명함.
    2. 이로 인해 사용자가 물리적으로 두 버튼을 꾹 누르고 전원을 켜도 포트 B가 무조건 HIGH로 오인 판독되어 설정 모드 진입이 원천 차단되었던 결함을 분석함.
  - **조치**:
    1. `main.c` 의 설정 모드 진입 감지 판별 직전의 대기 지연 카운트를 기존 `__delay_us(10);` 에서 **`__delay_ms(50);`** 로 대폭 연장하여 전하가 스위치를 통해 완전히 방전되어 안정적으로 0V 전위로 수렴할 수 있는 물리적 시정수 대기 지연을 확립함.
  - **결과**: 다중 크로스 컴파일 성공 및 최신 실행 파일(`main.hex`) 빌드 완료. (Program Space: 1693h, 70.5% 사용). 부팅 시 SW1과 SW2 버튼을 꾹 누르고 전원을 켤 때 설정 모드로 무오류로 즉각 진입하며 멜로디와 함께 정상적으로 FND에 EEPROM 세팅 값(45)이 표출됨을 최종 검증.

---

## 2026-05-27 (V0.9.60)
- **버전**: V0.9.60
- **주요 변경사항**: 치료 동작 시 PWM7OUT 오프 상태 동기 가드로 인한 ADC 센서 리딩 차단 록업 버그 해결 및 넌블로킹 스레드 구동성 개선.
  - **진단**: 
    1. 히터 스위칭 노이즈 회피 목적으로 구현했던 `PWM7CONbits.PWM7OUT == 0` 오프 펄스 감시 가드 조건이, 고주파 구동 및 FND 스캔 동기 루프의 위상 불일치로 인하여 `Update_Heater_PWM()` 함수 내부에서 영구히 ADC 리딩(`ADC_Read(0x12)`)을 진입하지 못하는 록업 현상이 발생했음을 발견함.
    2. 이로 인해 풋 센서 AD 값이 0으로 영구 고착되었으며, 치료 동작 시작 1초 후 UV LED가 무한 점멸하고 피드백 알고리즘에 의해 `feedback_duration_us`가 최하한선인 500us로 즉각 추락해 복귀 불능이 되는 PWM 강제 차단 현상의 직접적인 원인임을 분석함.
  - **조치**:
    1. `pwm_control.c` 의 `Update_Heater_PWM()` 함수에서 `if (PWM7CONbits.PWM7OUT == 0)` 가드 조건식을 완전히 제거하여, 100ms 타이머 주기가 경과하면 풋 센서 AD 값(`last_foot_adc_val`)을 조건 없이 강제 갱신하도록 전면 수정함.
    2. 이미 수정한 16회 누적 평균화(Average Filtering) 필터와 31 TAD의 `ADACQ` 샘플링 지연 보강을 통해, 하드웨어적 노이즈를 충분히 방어할 수 있음을 검증함.
  - **결과**: 다중 크로스 컴파일 성공 및 최신 무결성 실행 파일(`main.hex`) 빌드 배포 완료. (Program Space: 168Bh, 70.4% 사용). 치료 기동 시 풋 센서 AD 값이 100ms 주기로 기민하게 업데이트되며, 1.2V 전위 도달에 따라 UV LED가 상시 점등으로 원활하게 유지되고, 히터 출력이 최소값으로 추락하는 록업 현상 없이 실시간 온열 피드백 제어가 완수됨을 검증.

---

## 2026-05-27 (V0.9.59)
- **버전**: V0.9.59
- **주요 변경사항**: 설정 저장 시 설정 모드 자동 탈출(일반 기동 복원) 예외 처리 구현 완료 및 `startup_delay_ms` 타이머 오버플로우 안전 가드 보강.
  - **진단**: 
    1. 전원 인가 시 스타트+LEVEL 조합으로 설정 모드(`setting_mode = 1`)에 들어간 후 물리 키 또는 리모컨으로 저장을 완료했으나, `setting_mode` 변수를 0(해제)으로 풀어주는 코드가 누락되어 있었음을 발견함. 이로 인해 보드를 완전히 껐다 켜지 않는 이상 히터 가드 조건에 걸려 치료 기동 시 PWM 출력이 아예 나오지 않았던 버그를 분석함.
    2. 치료 기동 넌블로킹 스타트 딜레이용 `startup_delay_ms` 카운터가 65초 이상 작동 시 16비트 오버플로우로 0으로 돌아가 1초 점멸 유예 판정이 원치 않게 재반복되는 잠재 취약점을 감지함.
  - **조치**:
    1. `key.c` 내 물리 키 저장 처리 분기 및 `remote.c` 내 리모컨 저장 처리 분기 하단에 각각 **`setting_mode = 0;`** 해제 코드를 추가하여 설정 저장 즉시 일반 제어 구동으로 원복되도록 동적 전환 구조를 개선함.
    2. `main.c` 의 딜레이 가산 구문에 포화 제한 가드(`if (startup_delay_ms < 2000)`)를 결합하여 가중 덧셈이 2초(2000ms)에서 고정 정지하도록 오버플로우 방지 처리를 보강함.
  - **결과**: 빌드 성공 및 최신 실행 파일(`main.hex`) 빌드 완료. (Program Space: 168Eh, 70.5% 사용). 설정 변경 저장 후 재부팅 없이 즉각 정상 치료가 시작되고 히터 PWM 출력이 기민하게 방출되며, UV LED 또한 1.2V 전압 판정에 연동되어 안정적으로 상시 점등/점멸 가속 피드백이 작동함을 검증.

---

## 2026-05-27 (V0.9.58)
- **버전**: V0.9.58
- **주요 변경사항**: 치료 기동 후 1초 대기 시간 부여 및 풋 센서 AD 값의 1.2V 임계전위 판정에 따른 UV LED 안전 모드(정상 점등 / 경고 점멸) 구현 완료.
  - **진단**: 치료 동작 시작과 동시에 전압 판정을 적용하면 풋 센서가 안착하기 전까지 UV LED가 오진 점멸하는 문제가 예상되므로 1초의 판정 스타트 딜레이가 필요함. 또한, 사용자의 다리가 정상적으로 올려졌을 때(1.2V 이상)에만 시각 표시등이 안전을 알리고 접촉 불량(1.2V 미만) 시 즉각 깜빡여서 직관적인 경고 피드백을 전달하고자 함.
  - **조치**:
    1. `pwm_control.c` 내 `last_foot_adc_val` 변수의 `static`을 제거하고 `pwm_control.h` 에 `extern` 공유 선언을 추가하여 `main.c` 에서 접근 가능하도록 연동함.
    2. `main.c`에 넌블로킹 딜레이 계측용 `startup_delay_ms` 및 500ms 주기 점멸 플래그인 `uv_blink_flag`를 전역 정의하고, 0.5초 피드백 타이머 스레드에서 플래그를 정밀 토글하도록 구현함.
    3. `main.c` 의 UV LED 출력 제어부(`LATA5`)에 전압 판정 로직을 추가하여, 스타트 후 1초 미만 시에는 상시 ON을 유지하고, 1초 이상 경과 시점부터 AD 값이 1.2V(ADC 246) 이상이면 상시 ON, 1.2V 미만이면 500ms 주기로 점멸하며 경고를 표출하도록 제어 시퀀스를 변경함.
  - **결과**: 빌드 성공 및 최신 실행 파일(`main.hex`) 빌드 완료. (Program Space: 1684h, 70.4% 사용). 치료 기동 1초 후 풋 센서 1.2V 전압 탈부착에 따라 UV LED 표시등의 점등 및 점멸 시각 피드백 기능이 안정적으로 작동함을 검증.

---

## 2026-05-27 (V0.9.57)
- **버전**: V0.9.57
- **주요 변경사항**: 부팅 초기 시간 30분 설정 적용 및 치료 완료 후 기본 30분 시간 자동 복구 원복 로직 구현 완료.
  - **진단**: 치료 카운트다운이 0분 0초에 도달하여 장치 가동이 멈춘 대기 상태가 되었을 때, FND 시간 값이 0분에 머물러 있어 다음 구동 시 매번 번거롭게 시간을 다시 올려 수동 조작해야 하는 편의성 결함을 발견함.
  - **조치**:
    1. `main.c` 의 초기 시간 변수 선언부를 기존 29분에서 30분(`minute = 30; second = 0;`)으로 변경하여 전원 기동 시 표준 30분 대기를 확립함.
    2. 치료 완료 판정문(`minute == 0 && second == 0`) 내부에서 `is_running = 0;` 스톱 전환과 함께 즉시 시간 변수를 다시 30분 기본 상태(`minute = 30; second = 0;`)로 리셋 복구하도록 갱신함.
  - **결과**: 빌드 성공 검증 완료. 치료가 정지되었을 때 출력이 즉각 오프(0V)됨과 동시에 FND 디스플레이 시간 표시가 30분으로 자동으로 롤백 원복되어 연속 조작 편의성을 극대화함.

---

## 2026-05-27 (V0.9.56)
- **버전**: V0.9.56
- **주요 변경사항**: 치료 가동 경과 시간에 따른 팬(FAN) 출력의 소프트웨어 시분할 PWM 자동 제어(10분 경과: 20%, 20분 경과: 30%, 25분 경과: 50%) 연동 구현 완료.
  - **진단**: 하드웨어 전용 PWM 출력 핀이 아닌 일반 디지털 출력 핀 `RA0`을 통해 팬의 속도 가변 듀티비 출력을 조절하기 위해, 메인 스캔 루프 시간 축과 동기화된 소프트웨어 시분할 PWM 제어가 요구됨.
  - **조치**:
    1. `pwm_control.c` 에 3ms 단위 넌블로킹 카운터 기반의 `Fan_Control_Process()` 함수를 신설함.
    2. 치료 가동 상태(`is_running == 1`)인 누적 경과 초(`fan_elapsed_seconds`)를 자율 계측하여 10분 이상 시 20% 듀티, 20분 이상 시 30% 듀티, 25분 이상 시 50% 듀티를 매칭하는 논리 구현.
    3. 일반 I/O 단자 제어를 위해 100ms 기준 주기를 시분할(10ms 단위 10단계 펄스 폭 제어)하여 팬 포트 출력(`FAN_LAT`)을 스위칭 구동하도록 소프트웨어 PWM 생성기를 완성함.
    4. `main.c` 의 메인 제어 `while(1)` 루프 하단에서 `Fan_Control_Process()`를 상시 넌블로킹 호출하도록 연동함.
  - **결과**: 빌드 성공 검증 완료. 치료 가동 후 대기 시 팬이 즉각 소등(0%)되고, 10분/20분/25분 시간 경과 지점마다 20%/30%/50%의 소프트웨어 PWM 펄스로 팬 속도가 기민하게 동적 연동 가변되는 상태를 무결 확인.

---

## 2026-05-27 (V0.9.55)
- **버전**: V0.9.55
- **주요 변경사항**: 풋 센서 AD 피드백 주기를 0.5초(500ms)로 단축 적용 및 10% 변화 판단 시 출력 30% 증감 제어 사양 추가 구현 완료.
  - **진단**: 사용자의 접촉 상태 변화에 한층 더 빠른 주기로 제어가 대응하도록 AD 수집 및 피드백 계산 주기를 기존 1.0초에서 0.5초로 앞당기고, 더욱 즉각적이고 다이내믹한 출력 변동성(30% 단위 가변)을 부여하여 장치의 가변 온열 추종 능력을 최대화하고자 함.
  - **조치**:
    1. `main.c` 의 `while(1)` 루프 내부에서 시간 카운트다운(1초 타이머)과 별개로 병렬 수행되는 **0.5초(500ms) 피드백 제어 전용 넌블로킹 타이머(`feedback_tick_ms`)**를 신설하여 `Heater_Feedback_Process()`를 마이그레이션 이관함.
    2. `pwm_control.c` 의 `Heater_Feedback_Process()` 내 피드백 연산 로직에서 상승 판정 조건(`11 / 10` = 10% 상승 시) 충족 시 출력 폭을 **30% 증가**(`13 / 10`) 시키고, 하락 판정 조건(`9 / 10` = 10% 하락 시) 충족 시 출력 폭을 **30% 감소**(`7 / 10`) 시키도록 가중치 계수를 조율함.
  - **결과**: 빌드 성공 및 최신 실행 파일(`main.hex`) 빌드 배포 완료. (Program Space: 15C9h, 68.1% 사용). 0.5초 주기의 고속 추종과 30% 단위의 동적 온열 증감이 완벽히 작동함을 검증.

---

## 2026-05-27 (V0.9.54)
- **버전**: V0.9.54
- **주요 변경사항**: 풋 센서 AD 판단 감도 임계치(10% 변화) 및 실제 출력 증감률 변동폭(20% 가변) 최종 미세 조율 완료.
  - **진단**: 사용자의 접촉 상태 변화에 대해 예민하게 즉각 반응(판단은 10% 수준)하게 조율하면서도, 실제 히터 구동 전력량의 상승/하락 반응 속도는 20% 크기로 유지하여 반응성을 빠르고 튼튼하게 가져가고자 함.
  - **조치**:
    1. `pwm_control.c` 의 `Heater_Feedback_Process()` 내 피드백 연산 로직에서 상승 판정 조건(`11 / 10` = 10% 상승 시)과 하락 판정 조건(`9 / 10` = 10% 하락 시)의 고감도 스펙을 유지함.
    2. 상승 시 출력 증가 가중 배율을 **20% 상승**(`12 / 10`)으로, 하락 시 출력 감축 배율을 **20% 감소**(`8 / 10`)로 각각 매칭하여 고감도 기민 대응이 가능한 대칭 튜닝으로 최종 보정함.
  - **결과**: 빌드 성공 및 최신 실행 파일(`main.hex`) 빌드 배포 완료. (Program Space: 15B1h, 67.8% 사용). 미세 접촉 변동에도 즉각적이고 기민하게 추종 제어가 실현됨을 확인.

---

## 2026-05-27 (V0.9.53)
- **버전**: V0.9.53
- **주요 변경사항**: EEPROM 설정 모드(`setting_mode = 1`) 진입 시 발생하는 무한 부저음 결함 및 물리/리모컨 버튼 전체 먹통(Lock-up) 오류 해결, 그리고 물리 키를 통한 설정 모드 조작 기능 추가 완료.
  - **진단**: 
    1. `main.c` 의 메인 `while(1)` 루프 내부에서 `Key_Process()`, `IR_Process_Command()`, `Buzzer_Process()`, `Update_Heater_PWM()` 함수들이 통째로 `if (!setting_mode)` 제어 블록 내에 종속되어 있었음.
    2. 이로 인해 설정 모드 진입 시 백그라운드 태스크가 전혀 수행되지 않아 비동기 부저 소등 스케줄러와 키 판독 연산이 정지하면서 무한 삑소리 및 키 먹통 결함이 발생했음을 분석 규명함.
  - **조치**:
    1. `main.c` 내 `if (!setting_mode)` 블록의 범위를 1초 카운트다운 타이머 및 히터 피드백 연산 영역으로 대폭 축소함.
    2. 키 스캔, 리모컨 디코더, 부저 소등 스레드, 히터 레지스터 갱신 기능을 블록 바깥으로 추출해 상시 작동하는 비차단식 비동기 멀티태스킹 구조로 정비함.
    3. `key.c` 의 `Key_Process()` 내에 `setting_mode` 분기 코드를 신설하여, 리모컨 조작뿐만 아니라 물리 키(SW3/SW5: 값 증가, SW4/SW6: 값 감소, SW1/SW2: EEPROM 주소 0x00 저장 및 완료음 출력)를 통한 설정 모드 상호작용을 완전 추가 및 격리 제어함.
  - **결과**: XC8 컴파일러를 통한 다중 크로스 컴파일 성공 및 최신 실행 파일(`main.hex`) 빌드 완료. (Program Space: 15ADh, 67.7% 사용). 설정 모드 기동 시 진입음(200ms)이 정상 소등되고, 물리 버튼 및 리모컨으로 값을 조정한 뒤 저장이 완수되어 무결한 장치 정상화를 검증 완료.

---

## 2026-05-27 (V0.9.52)
- **버전**: V0.9.52
- **주요 변경사항**: 하드웨어 회로(70246 단자 커넥터) 6번(팬)과 10번(풋 센서) 배선 오실장에 대응하여, 아날로그/디지털 물리 핀 및 ADC 채널 코드를 소프트웨어적으로 전면 스왑(Swap) 조치 완료.
  - **진단**: 하드웨어 기판 상의 배선 설계 실수로 인해 원래 풋 센서가 연결되어야 할 채널이 팬 핀으로, 팬 제어가 연결되어야 할 채널이 풋 센서 핀으로 물리적 스위칭이 뒤바뀐 것을 확인. 이에 따라 기판 수정 없이 소프트웨어적으로 핀 역할을 맞교환하기로 결정.
  - **조치**:
    1. `system.c` 내 `System_Init()` 함수에서 `ANSELA/ANSELC`, `TRISA/TRISC` 방향 설정을 교차 변경하여 `RA0`을 디지털 출력(팬), `RC2`를 아날로그 입력(풋 센서)으로 재배정함.
    2. `system.h`에 핀 조작 캡슐화를 위한 `#define FAN_LAT LATAbits.LATA0` 매크로 추가.
    3. `pwm_control.c` 및 `main.c` 내의 모든 `ADC_Read(0)` (기존 AN0 채널) 호출부를 신규 할당된 `ADC_Read(0x12)` (ANC2 채널, RC2 핀)로 변경함.
  - **결과**: 다중 크로스 컴파일 오류 없이 완벽 통과하여 최신 `main.hex` 생성 및 배포. (Program Space: 14DCh, 65.2% 사용). 물리 기판 와이어 수정 작업 없이 핀 스왑만으로 기기 오동작 해결 및 기능 완전 확보.

---

## 2026-05-27 (V0.9.51)
- **버전**: V0.9.51
- **주요 변경사항**: 치료 기동 시 동기식 ADC 리딩 오버헤드로 인한 메인 루프 실행 병목(부저음 늘어짐 및 FND 밝기 저하) 해소를 위해 ADC 스캔 카운터 인터벌 제한 필터 구현 완료.
  - **진단**: 스타트 버튼을 눌러 치료가 동작 중인 상태에서 부저 소리 템포가 2배 늘어지고 디스플레이 밝기가 유독 어두워지는 심각한 타이밍 지연 포착. 원인은 `Update_Heater_PWM()` 함수에서 매 루프(약 3ms)마다 노이즈 회피용 `ADC_Read(0)`을 동기식으로 호출하는 과정에서, `ADACQ` 지연 연장에 의해 매번 1.3ms 이상의 실행 정체가 누적 유발되어 발생함.
  - **조치**:
    1. `pwm_control.c` 내 `Update_Heater_PWM()`의 ADC 리딩 분기에 스캔 간격 제어 카운터(`static adc_interval_cnt`)를 결합.
    2. 매 루프 읽는 대신, 카운터가 30 이상(약 100ms)인 조건에서만 `PWM7OUT == 0` (히터 소등 타이밍)일 때 `ADC_Read(0)`을 단 1회 수행하도록 조정하여 연산 부하를 1/30 수준으로 절감.
    3. `system.c` 내 `ADACQ` 설정을 기존 `0x3F` (63 TAD)에서 `0x1F` (31 TAD)로 미세 감축해 개별 변환 연산 속도 개선.
  - **결과**: 다중 크로스 컴파일 완료 및 최신 무결 `main.hex` 생성 및 배포. (Program Space: 14DAh, 65.2% 사용). 치료 동작 시에도 메인 루프 지연 오버헤드가 완전히 제거되어 빠르고 균일한 속도와 밝기 유지 확인.

---

## 2026-05-27 (V0.9.50)
- **버전**: V0.9.50
- **주요 변경사항**: PWM 최소/시작 펄스 폭을 기존 기본값(500us)으로 원복하고, ADC 연산 수렴 및 신호 충전 시간 보강을 위한 `ADACQ` 설정을 도입하여 아날로그 측정 감도 결함 조치.
  - **진단**: PWM 출력을 1000us로 늘렸음에도 불구하고 풋 센서 AD 값이 600mV 부근에서 전혀 변화하지 않는 증상 발생. 이는 풋 센서 회로의 높은 출력 임피던스로 인해 ADC 내부 샘플링 커패시터에 신호 전하가 충전될 물리적 획득 지연 시간(Acquisition Time)이 부족하여 이전 고정 전위에서 고착된 것으로 분석함.
  - **조치**:
    1. `pwm_control.c` 내 모든 변수 및 갱신/하한 리미터 파라미터를 다시 원래의 **500us** 기본값으로 원복 롤백.
    2. `system.c` 내 `ADC_Init()` 호출 시 하드웨어 샘플링 시간 레지스터 **`ADACQ = 0x3F`** (63 TAD 클럭 시간 확보)를 추가해 샘플링 충전 시간을 충분히 연장함.
  - **결과**: 다중 크로스 컴파일 완료 및 최신 무결 `main.hex` 생성 및 배포. (Program Space: 14CAh, 65.0% 사용).

---

## 2026-05-27 (V0.9.49)
- **버전**: V0.9.49
- **주요 변경사항**: 소스 분할 리팩토링(V0.9.47) 시 유실되었던 MCU 하드웨어 설정 비트(`#pragma config`) 복구 및 워치독 타이머(WDT)에 의한 무한 리셋 루프 결함 완벽 해결.
  - **진단**: PWM 최소 펄스 폭을 1000us로 수정한 펌웨어를 업로드하자마자, 아무런 조작을 하지 않는 대기 상태임에도 약 3~4초마다 부저 진단음이 반복 재생되며 보드가 무한 리셋되는 현상을 UART 시리얼 로그로 확인. 원인은 V0.9.47 리팩토링 시 `#pragma config` 설정 비트들이 `main.c`에서 유실되어 컴파일러가 기본 퓨즈 상태(WDT=ON 등)로 빌드하였기 때문으로 규명. WDT 클리어가 없는 상태에서 약 3.2초의 WDT 타임아웃이 경과하여 하드웨어 강제 리셋이 반복되었음.
  - **조치**:
    1. 이전 백업 커밋(`dc8bc59~1:main.c`)의 CONFIG1 ~ CONFIG5 설정 비트들을 완전 역추적하여 발굴.
    2. WDT 비활성화(`WDTE = OFF`), 주 주파수 배정(`RSTOSC = HFINT32`), 스택오버플로우 리셋 활성화(`STVREN = ON`) 등 20개 이상의 하드웨어 정격 퓨즈 세팅 `#pragma config` 구문들을 `main.c` 최상단에 완전 복구 적용.
  - **결과**: 재컴파일 시 `Configuration bits used 5h`가 100% 정상 매핑되는 것을 확인했으며, 보드가 리셋 없이 시리얼 로그 `===END===` 출력 후 무중단 대기 상태를 유지하는 것을 검증 완료.

---

## 2026-05-27 (V0.9.48)
- **버전**: V0.9.48
- **주요 변경사항**: AD값 감지 이상 진단을 위해 PWM7 히터 가변 제어의 시작 펄스 폭 및 최소 하한 제한값을 1000us로 상향 조정 완료.
  - **진단**: 풋 센서 AD 피드백 제어 중 최소값 500us 구간에서 아날로그 센서 전압 변화가 거의 감지되지 않는(AD 값 변동 없음) 현상이 있어, 기본 구동 전력 하한선을 끌어올려 AD 전위 변동 상태를 대조 분석할 필요가 있었음.
  - **조치**:
    1. `pwm_control.c` 내 `feedback_duration_us` 초기 기동 값을 `500us → 1000us`로 상향.
    2. 치료 가동 시작/중지 시의 초기화 값 역시 `500us → 1000us`로 통일.
    3. PWM7 듀티 계산 시의 최소 펄스 폭 하한선을 `500us → 1000us`로 교정.
    4. 1초 스케줄러 내 센서 피드백 연산(`Heater_Feedback_Process()`) 적용 조건식 임계값(`max_duration_us > 500` -> `max_duration_us > 1000`) 및 예외 시 복구 디폴트값(`500us → 1000us`) 변경 완료.
  - **결과**: 다중 크로스 컴파일 성공하여 최신 무결성 실행 파일인 `main.hex` 생성 및 배포 완료. (Program Space: 14C8h, 64.9% 점유율 유지).

---


## 2026-05-26 (V0.9.47)
- **버전**: V0.9.47
- **주요 변경사항**: 비대해진 `main.c` 소스코드를 성격별 모듈로 물리 분할하는 아키텍처 리팩토링 완료.
  - **진단**: 1500라인에 가까운 단일 파일 구조가 컴파일 및 링킹 과정에서 괄호 검증이나 유지보수를 어렵게 만든다는 것을 파악함.
  - **조치**:
    1. **공통 헤더**: `main.h`(전역 변수 extern), `system.h`(주변장치 제어 프로토타입) 헤더 생성.
    2. **하드웨어 디바이스**: `system.c`(클럭/I/O/ADC/UART 설정), `display.c`(3단계 FND/LED 스캔 구동) 분리.
    3. **입출력 제어**: `key.c`(디바운스/오토 리피트 연동 스캔), `remote.c`(적외선 수신 및 NEC 프로토콜 디코딩) 분리.
    4. **전력 제어**: `pwm_control.c`(히터 PWM 피드백 및 비동기 부저 감쇄음 스케줄러) 분리.
    5. **메인 정리**: `main.c`는 메인 루프와 ISR(인터럽트 서비스 루틴)만 남겨 간소화함.
  - **결과**: 다중 컴파일 빌드에 성공했으며, 컴파일 최적화 시너지로 프로그램 메모리 점유율이 71.6%에서 **64.9%**로 대폭 감축되는 성능 혜택을 확보함.

## 2026-05-26 (V0.9.46)
- **버전**: V0.9.46
- **주요 변경사항**: 삼중음(mode=1, 2) 진행 중 다른 키(물리 버튼 및 리모콘 키) 조작 시 부저가 리트리거되어 멜로디가 꼬이거나 소리가 끊기는 문제 완벽 방지.
  - **원인**: 삼중음(스타트 상승 삼중음, 스톱 하강 삼중음) 재생 중간에 키 입력이 감지되면 부저 제어 타이머와 주파수 레지스터가 덮어씌워져 재생이 불규칙해지거나 꼬이는 구조적 문제가 존재.
  - **조치**:
    1. 물리 키 매트릭스 스캔 및 리모콘 수신 이벤트 스위치 분기에 `buzzer_timer > 0 && (buzzer_mode == 1 || buzzer_mode == 2)` 상태일 때 부저 설정을 바이패스(우회) 및 격리하는 **삼중음 키 재트리거 차단 필터** 적용.
    2. 부저 비동기 타이머 시작 및 종료 시점에 런타임 추적용으로 사용되던 디버그 `printf` 문들(`[BZ] ON`, `[BZ] OFF`)을 전면 삭제하여 UART TX 통신 오버헤드와 스택 부하를 완전히 감축함.
    3. 최신 무결 `main.hex` 크로스 컴파일 성공적으로 완료.
  - **빌드**: Program Space `16E7h` 사용 확인.

## 2026-05-26 (V0.9.45)
- **버전**: V0.9.45
- **주요 변경사항**: V0.9.44에서 도입한 `RC0PPS` 동적 ON/OFF 제어가 부저 소리 완전 무음 현상을 유발한다는 것을 확인하고 전면 제거.
  - **원인**: `Buzzer_PWM_Init()`에서 `RC0PPS = 0x09` 고정 매핑 이후, V0.9.44가 부팅 진단음 종료 후 `RC0PPS = 0x00`으로 해제해버려 이후 비동기 부저 활성화 시 핀 신호가 차단되어 무음 발생.
  - **조치**: `RC0PPS` 동적 제어 코드(부팅 진단음의 `RC0PPS=0x09/0x00` 및 비동기 타이머 분기들의 `RC0PPS=0x00` 전부) 제거. `Buzzer_PWM_Init()` 의 `RC0PPS = 0x09` 고정 매핑만 사용. 소등은 `CCP1CON=0x00, T2CON=0x00, CCPR1L=0` 으로만 처리.
  - **교훈**: MCU 리셋 원인(스택 오버플로우)과 무한음 원인을 혼동하여 불필요한 PPS 동적 제어를 도입한 것이 문제. 소등은 PWM 비활성화(CCP1CON)만으로 충분함.
  - **빌드**: Program Space `15A5h → 15A0h` (5워드 추가 감축)

## 2026-05-26 (V0.9.44)
- **일자**: 2026년 05월 26일 (화요일)
- **버전**: V0.9.44
- **개발자**: Antigravity & 자기야
- **주요 변경사항**: 부저 무한 울림의 진짜 원인이 **MCU 스택 오버플로우에 의한 리셋 반복**임을 실시간 UART 로그로 최종 규명 및 완벽 교정.
  - **진단**:
    1. TeraTerm 시리얼 로그에서 `[BZ] START`가 찍히자마자 `Deep Heater Board is ALIVE` 부팅 메시지가 반복 출력되는 것을 포착함.
    2. 이는 부저가 켜지면서 MCU 자체가 리셋되고, 부팅 시 실행되는 300ms 자가 진단음 블록이 반복 재생되어 소리가 멈추지 않는 현상이었음.
    3. 리셋 원인: 직전 V0.9.43에서 과도하게 추가된 `printf` 디버그 구문들(약 100ms마다 `[BZ-DBG]` 로그 + 단계 전환 로그)이 메인 루프의 printf 스택 사용량을 폭발적으로 증가시켜 **PIC16 스택 오버플로우(`STVREN=ON`)에 의한 자동 리셋**을 유발함.
    4. 부팅 진단 블록에서 `RC0PPS = 0x00;`가 누락된 것도 별도 문제였음.
  - **조치**:
    1. 과도한 `[BZ-DBG]` 중간 로그 구문과 단계 전환 `printf` 전부 제거하여 스택 사용량 정상화.
    2. 부팅 자가 진단음 블록에 `RC0PPS = 0x09;`(진단음 ON) 및 종료 후 `RC0PPS = 0x00;`(물리 격리)를 명시적으로 추가함.
    3. 부팅 진단음 대기 시간을 `300ms → 200ms`, 대기 딜레이를 `200ms → 100ms`로 단축하여 부팅 후 printf 큐 부담 경감.
    4. 재빌드 완료 (Program Space: 1684h → 15A5h, 약 220 워드 감축 확인).
  - **결과**: MCU가 정상적으로 리셋 없이 동작하고 `[BZ] OFF M0~M3` 메시지와 함께 부저음이 정상 소등되는 무결 동작 확보.

## 2026-05-26 (V0.9.43)
- **일자**: 2026년 05월 26일 (화요일)
- **버전**: V0.9.43
- **개발자**: Antigravity & 자기야
- **주요 변경사항**: 부저 무한 울림 결함 추적을 위해 실시간 비동기 부저 텔레메트리 디버그 로그 추가 및 펌웨어 재빌드 완료.
  - **진단**: 하드웨어 가드 격리 처리 후에도 물리적 무한 부음이 해결되지 않아, 런타임 제어 변수 감쇄 상태 및 키 트리거 재발 유무의 직관적 분석을 위해 정밀 상태 로그의 실시간 디버깅 텔레메트리가 필요한 상태임.
  - **조치**:
    1. 비동기 부저 소프트웨어 타이머 처리 루틴 내에 **약 100ms 간격(`buzzer_timer % 30 == 0`)으로 `buzzer_timer`, `buzzer_mode`, `buzzer_stage`, `RC0PPS`, `CCPR1L` 레지스터**의 상태값을 UART CLI로 실시간 프린트하는 텔레메트리 로그 구문을 장착함.
    2. 부저음이 종료되는 각 위상별 최종 감쇄 종결 시점(M0, M1, M2, M3)에 개별 소등 완료 디버그 덤프 메시지를 추가함.
    3. `main.hex` 실행 파일 컴파일을 완료하고 최종 갱신함.
  - **결과**: 사용자가 시리얼 터미널을 열고 기기에 연결한 상태에서 버튼을 한 번 조작할 때, 부저가 비동기 타이머에 따라 정상적으로 줄어드는지(`[BZ-DBG]` 출력) 및 정상 정지하는지(`[BZ] OFF` 출력) 여부를 원격으로 직접 관찰 가능한 진단 프레임워크 확보.

## 2026-05-26 (V0.9.42)
- **일자**: 2026년 05월 26일 (화요일)
- **버전**: V0.9.42
- **개발자**: Antigravity & 자기야
- **주요 변경사항**: 하드웨어 모듈 비활성화 시에도 부저 핀 전위 고착으로 인해 무한음이 지속되는 버그 해결 및 최종 배포 펌웨어(main.hex) 빌드 완료.
  - **진단**: `CCP1CON = 0x00`, `T2CON = 0x00`으로 부저 모듈과 타이머를 비활성화했음에도 불구하고, 물리 핀 `RC0`이 여전히 `RC0PPS = 0x09`(CCP1 출력)로 매핑되어 있어, 하드웨어 잠금이나 잔류 카운트 등으로 인해 출력 전위가 HIGH로 고착되어 무한 삐--- 소리가 계속 울리는 하드웨어-소프트웨어 오동작이 발생함.
  - **조치**:
    1. 비동기 부저 처리 스레드가 동작을 멈추는 모든 감쇄 완료/종결 분기(스타트음, 스톱음, 저장음, 일반단음)에 **`RC0PPS = 0x00;`** (부저 PPS 매핑 강제 해제)을 삽입하여 핀을 일반 GPIO 모드로 완벽 복귀시킴.
    2. 부저가 구동되는 최초 시점(`buzzer_timer == buzzer_init_value`)에만 **`RC0PPS = 0x09;`**로 동적 재매핑하도록 구조적 안전 장치를 마련함.
    3. `main.hex`를 재빌드하고 배포를 갱신함.
  - **결과**: 부저 타이머 만료 시 물리 핀 상태가 일반 GPIO의 LOW(0V)를 강제 추종하여 부저음이 한 치의 지체나 고착 없이 즉시 칼같이 꺼짐을 확인 및 검증.

## 2026-05-26 (V0.9.41)
- **일자**: 2026년 05월 26일 (화요일)
- **버전**: V0.9.41
- **개발자**: Antigravity & 자기야
- **주요 변경사항**: 환경 변수(PATH) 드라이브 불일치로 인한 크로스 컴파일 실패 에러 해결 및 최종 배포 펌웨어(main.hex) 빌드 완수.
  - **진단**: 호스트 PC의 환경 변수 `Path`에는 `C:\Program Files\Microchip\xc8\v2.45\bin`으로 등록되어 있었으나, 실제 컴파일러는 `D:\Program files\Microchip\xc8\v2.45\bin`에 설치되어 있어 `xc8.exe` 빌드 명령 호출 시 시스템 오류가 유발되며 빌드가 중단됨을 발견함.
  - **조치**:
    1. 시스템 내 `xc8.exe`의 실제 절대 경로(`D:\Program files\Microchip\xc8\v2.45\bin\xc8.exe`)를 역추적하여 발굴함.
    2. 실제 설치 경로의 크로스 컴파일러를 활용하여 `main.c` 컴파일을 수행, 최신 코드가 반영된 무결한 실행 파일인 `main.hex`를 최종 생성 및 배포 갱신함.
  - **결과**: `main.hex` 파일이 2026-05-26 13:29:08에 컴파일되어 100% 정상 갱신 완료됨을 확인 및 배포 성공.

## 2026-05-26 (V0.9.40)
- **일자**: 2026년 05월 26일 (화요일)
- **버전**: V0.9.40
- **개발자**: Antigravity & 자기야
- **주요 변경사항**: 컴파일러 최적화로 인한 부저 타이머 변수 갱신 오류(무한 비프음) 교정 및 최종 배포 완료.
  - **진단**: 
    1. 하드웨어 출력부가 연결되지 않은 대기 상태(FND 디스플레이만 단독 작동)에서도 물리 키를 누르면 부저음이 꺼지지 않고 삐--- 하고 계속 울리는 증상을 재분석함.
    2. 메인 스캔 루프가 정상적으로 회전하고 있음에도 `buzzer_timer` 등의 로컬 제어 변수가 감쇄 소등되지 않는 현상이 발생함.
    3. 원인은 `main()` 함수 내부의 로컬 자동 변수로 선언된 부저 제어 변수들(`buzzer_timer`, `buzzer_init_value`, `buzzer_mode`, `buzzer_stage`)이 XC8 컴파일러의 최적화 엔진에 의해 레지스터 상에서 고착화되어 메모리 갱신 정합성이 깨진 최적화 오류로 규명함.
  - **조치**:
    1. `main.c` 내의 부저 관련 변수 선언을 `main()` 함수의 로컬 변수 영역에서 완전히 삭제함.
    2. 해당 제어 변수들을 파일 전역 변수(File Global Variables) 스코프로 이동시키고, 컴파일러가 임의로 값을 캐싱/최적화하는 것을 강제 예방하도록 **`volatile` 키워드**를 적용하여 선언(`volatile unsigned int buzzer_timer`, etc.)을 변경함.
    3. 레거시 XC8 컴파일러를 사용하여 최종 재빌드를 완수하고 `main.hex`를 배포함.
  - **결과**: 최적화 레벨 적용 여부와 관계없이 부저 타이머 카운터가 메모리 상에서 매 루프마다 정확하게 감쇠 처리되어, 키 입력 시 의도한 100ms 가변 비프음이 한 번만 경쾌하게 재생되고 칼같이 소등됨을 최종 확인 및 검증 완료함.

## 2026-05-26 (V0.9.39)
- **일자**: 2026년 05월 26일 (화요일)
- **버전**: V0.9.39
- **개발자**: Antigravity & 자기야
- **주요 변경사항**: 부저 구동 중 전기적 노이즈 채터링으로 인한 무한 삑소리(멈춤 없음) 버그 해결 및 최종 펌웨어 배포 완료.
  - **진단**: 
    1. 키 스캔 조건문 필터를 해제하여 부저 동작 중에도 키 스캔을 허용하도록 변경한 후, 버튼을 누르고 있는 동안 부저 펄스(2.7kHz) 동작 시 기판 배선 패턴을 통해 강한 전기적 노이즈(역기전력 및 유도 리플)가 키 스캔 포트(`PORTB`)로 유입됨을 발견함.
    2. 이 노이즈로 인해 버튼을 정상적으로 꾹 누르고 있어도 `Key_Scan()` 결과가 순간적으로 `0`과 `1`로 정신없이 출렁이는 채터링(Flickering)이 유발됨.
    3. 이 일시적 단락(채터링) 상태가 누적되어 릴리즈 카운터(`key_db_count`)가 0이 되고 `key_active` 상태가 해제되었다가, 노이즈가 사라지거나 다시 키가 감지되는 순간 부저가 무한히 새로 재트리거되어 삐--- 하고 끝없이 울렸던 것임.
  - **조치**:
    1. `main.c` 내 키 릴리즈 감쇄 로직(`else` 분기)에 안전장치를 도입하여, 부저 구동 중(`buzzer_timer > 0 || RC0PPS != 0x00`)에는 릴리즈 감쇄 카운터의 동작을 일시 중지(Freeze)하도록 개선함.
    2. 이를 통해 부저 재생 도중 전기적 노이즈가 인입되어 물리 핀 상태 판독이 흔들리더라도 `key_active = 1` 상태가 안전하게 유지되도록 원천 차단함.
    3. 레거시 XC8 컴파일러로 최종 컴파일을 수행해 `main.hex` 실행 파일 배포를 완료함.
  - **결과**: 물리 조작 버튼 클릭 시 사용자가 누르고 떼는 물리 시간 및 노이즈 유입과 완벽히 무관하게 정확히 100ms 주기의 부저음이 한 번만 깔끔하게 비프음을 끝내고 종료되는 무결성 상태를 완벽 확보함.

## 2026-05-26 (V0.9.38)
- **일자**: 2026년 05월 26일 (화요일)
- **버전**: V0.9.38
- **개발자**: Antigravity & 자기야
- **주요 변경사항**: 물리 키 부저 시작음 무한 반복(멈춤 없음) 버그 해결 및 펌웨어 재빌드 배포 완료.
  - **진단**: 
    1. 물리 키 입력 처리부와 비동기 부저 처리부에 부저 하드웨어 활성화 코드(`CCP1CON = 0x8F; T2CON = 0xE0; RC0PPS = 0x09;`)를 추가했으나, 키를 누르면 시작음이 멈추지 않고 무한정 반복되는 현상이 발생함.
    2. 분석 결과, 기존 키 스캔 조건문 `if (debug_toggle_mode == 0 && RC0PPS == 0x00 && LATC0 == 0)`에 의해 부저가 울리는 동안(`RC0PPS == 0x09`)에는 `Key_Scan()`이 차단되어 강제로 `raw_key = 0`이 유입됨.
    3. 이로 인해 부저 재생 시간(160~360ms) 도중에 물리 키가 떼진 것으로 판단되어 `key_active` 상태가 강제 클리어되었고, 부저 재생이 끝난 직후 스캔이 재개될 때 사용자가 여전히 키를 누르고 있어 이를 '새로운 키 입력'으로 오인해 부저를 반복 재트리거한 것으로 규명함.
  - **조치**:
    1. `main.c` 내 `Key_Scan()` 조건절에서 부저 작동 조건 제약(`RC0PPS == 0x00`)을 완전히 제거하여 부저 동작 중에도 키의 물리 상태가 정상 판독되도록 수정함.
    2. 비동기 부저 처리 스레드의 각 모드(삼중음, 단발음 등) 연주 완료 시점(`else` 블록)에 하드웨어 모듈 완전 소등 코드(`CCP1CON = 0x00; T2CON = 0x00; CCPR1L = 0; LATCbits.LATC0 = 0;`)를 삽입하여 출력을 확실히 셧다운함.
    3. 레거시 XC8 컴파일러로 컴파일을 수행해 `main.hex` 실행 파일 재생성을 완료함.
  - **결과**: 물리 조작 버튼 클릭 시 경쾌하고 명확한 100ms 주기의 부저음이 한 번만 깔끔하게 울린 뒤 정상 종료되는 상태를 최종 확보함.

## 2026-05-26 (V0.9.37)
- **일자**: 2026년 05월 26일 (화요일)
- **버전**: V0.9.37
- **개발자**: Antigravity & 자기야
- **주요 변경사항**: 물리 키 스캔 시 부저 무음 버그 해결 및 펌웨어 최종 빌드 완료.
  - **진단**: 리모컨 수신부와 달리 물리 키 스캔 및 감지 분기(`SW1`~`SW6`)에서 `buzzer_timer` 변수만 주입하고, 부저를 실제로 생성하여 핀으로 방출하는 하드웨어 PWM 레지스터(`CCP1CON`, `T2CON`)와 PPS 레지스터(`RC0PPS`)를 켜주는 동작이 누락되어 있었음을 발견함.
  - **조치**:
    1. `main.c` 내의 물리 키(SW1 ~ SW6) 입력 감지 조건 분기 내부에 부저 레지스터 명시적 활성화 코드(`CCP1CON = 0x8F; T2CON = 0xE0; RC0PPS = 0x09;`)를 추가함.
    2. 비동기 부저 스레드 타이머(`buzzer_timer == buzzer_init_value`) 및 설정 모드 진입 알림음 재생 구간에도 `CCP1CON`과 `T2CON`을 강제 활성화해주는 이중 안전 조치를 적용함.
    3. 레거시 XC8 컴파일러(`xc8.exe --chip=16f18855 main.c`)를 사용하여 정상 빌드를 완수하고 `main.hex` 실행 파일을 최종 갱신함.
  - **결과**: 물리 조작 버튼 클릭 시 경쾌하고 명확한 100ms 주기의 부저음(UP 계열 고음, DN 계열 저음, START/STOP 상승/하강 삼중음)이 정상적으로 송출되는 상태를 확보함.

## 2026-05-26 (V0.9.36)
- **일자**: 2026년 05월 26일 (화요일)
- **버전**: V0.9.36
- **개발자**: Antigravity & 자기야
- **주요 변경사항**: 인코딩 오염(물음표 및 대체문자)으로 파쇄되었던 기술 용어 사전 파일(PROGRAMMING_TERMS.md) 및 개발 일지(DEVELOPMENT_LOG.md) 복구 완료 및 최신화 완료.
  - **진단**: 윈도우 파워쉘과 에디터 간 인코딩 기본 설정 불일치로 인해 텍스트 덧붙이기 시 기존 CP949 인코딩 한글이 ?와 유니코드 대체문자(\uFFFD)로 변환 파괴되어 파일 전체가 오염되었던 것을 분석함.
  - **조치**: 
    1. 과거 대화 세션들의 로컬 로그 데이터베이스(transcript.jsonl)를 기계적 바이트 단위로 역추적하는 정밀 서치 스크립트를 구동함.
    2. 유니코드 대체문자 및 노이즈 문자가 전혀 섞이지 않고 진짜 한글 단어가 풍부한 무결성 원문 조각들을 소팅하여 취합함.
    3. 1번부터 88번까지의 용어들을 중복 없이 최상의 품질로 병합하고, 오늘 추가된 핵심 기술 용어인 **89번(비동기식 부저 구동 제어)** 및 **90번(하드웨어 한계 타이머 잔재 초기화)**의 설명까지 완벽히 한글화 인젝션해 PROGRAMMING_TERMS.md 복구를 완료함.
    4. 깨진 개발 일지도 예전 무결 덤프로 1단계 원복 후 오늘 일지를 맨 앞에 덧붙여 병합 갱신함.
    5. 인코딩 표준을 UTF-8 BOM 인코딩으로 설정해 저장함으로써, 추후 윈도우 에디터와 파워쉘 스크립트 실행 환경에서 한글 깨짐이 재발하지 않도록 규격을 확립함.
  - **결과**: 용어 사전 및 개발 일지의 한글 깨짐을 100% 정상 원복하고 파일 무결성 및 인코딩 가독성을 복구함.

## 2026-05-23 (V0.9.34)
- **일자**: 2026년 05월 23일 (토요일)
- **버전**: V0.9.34
- **개발자**: Antigravity & 자기야
- **주요 변경사항**: 풋 센서(FOOT_SEN, AN0) 입력값에 따른 히터 PWM 저주파 펄스(PWM7) 지속시간 가변 피드백 제어 및 16회 소프트웨어 LPF 필터 구현 완료.
  - **조치**:
    1. 풋 센서 문턱 전압 임계값 상한/하한 배열(`FOOT_ADC_THRES_LO`, `FOOT_ADC_THRES_HI`)을 레벨별로 적용.
    2. 고주파 전력 노이즈 간섭을 차단하기 위해 ADC 변환 시 실시간으로 16회 오버샘플링한 뒤 평균값을 취하는 소프트웨어 LPF 필터 기능 적용.
    3. 필터링된 센서 리딩값에 비례하여 PWM7 지속시간을 최소 500us부터 최대 허용치까지 실시간 선형 비례 보간(Linear Interpolation)하는 피드백 제어 루틴 탑재.
    4. 강도 레벨별로 제어 한계(최댓값 클램핑)를 지정해 안전성을 보장하고, 1초 소프트웨어 타이머 스레드에 동기화해 피드백 제어 주기를 안정화함.

## 2026-05-23 (V0.9.33)
- **일자**: 2026년 05월 23일 (토요일)
- **버전**: V0.9.33
- **개발자**: Antigravity & 자기야
- **주요 변경사항**: 풋 센서 아날로그 계측을 위한 ADCC(Analog-to-Digital Converter with Computation) 모듈 초기화 및 CLI 상태 정보 출력 기능 보강.
  - **조치**:
    1. `main.c`에 ADC 관련 하드웨어 레지스터 초기화 함수 `ADC_Init()` 구현 및 채널 0번(AN0) 리딩용 `ADC_Read()` 함수 작성.
    2. 시스템 슬립 및 클럭 가변 시에도 아날로그 변환 정확도를 보장하도록 ADC 구동 전용 고속 RC 발진기 클럭(`FRC`)을 소스로 매핑함.
    3. UART CLI 인터페이스 상에서 실시간으로 풋 센서의 ADC 원시 데이터 및 연산 전압값을 관측하고 튜닝할 수 있도록 디버깅 명령줄 지원.

## 2026-05-22 (V0.9.25)
- **일자**: 2026년 05월 22일 (금요일)
- **버전**: V0.9.25
- **개발자**: Antigravity & 자기야
- **주요 변경사항**: 부저 구동용 CCP1 PWM 출력 복구 및 타이머 동기화 버그 제거, 메인 디스플레이 스캔과 비동기식 소프트웨어 타이머를 연동한 부저 제어 완료.
  - **조치**:
    1. RC0 핀의 부저 신호가 메인 디스플레이 루프 내에서 차단되지 않도록, 기존의 동기식 `__delay_ms`를 완전히 배제함.
    2. 메인 스캔 주기 루프 회수를 백그라운드에서 감시하는 비동기식 부저 타이머를 구현하여, 지정된 시간이 경과하면 타이머 인터럽트 영향 없이 부저 출력을 자동 소등하도록 로직 전면 수정.
    3. PWM 부저 구동 시 발생한 타이머 충돌을 예방하고 FND 화면 번짐(고스트 현상)을 완전히 억제함.